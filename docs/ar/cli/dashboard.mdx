---
---
title: عرض الجلسات
description: "قم بتشغيل لوحة التحكم لاستعراض جلسات الوكيل وإدارة السياسات"
---

```bash
failproofai
```

يبدأ لوحة التحكم الويب على `http://localhost:8020`.

## الخيارات

| الراية | الوصف |
|------|-------------|
| `--port <number>` | المنفذ المراد الاستماع عليه (الافتراضي: `8020`) |
| `--allowed-origins <origins>` | قائمة مفصولة بفواصل من المضيفين/عناوين IP المسموح لها بالوصول إلى موارد التطوير |

لتوجيه لوحة التحكم إلى مجلد مشروع Claude غير افتراضي، قم بتعيين متغير البيئة `CLAUDE_PROJECTS_PATH` عند التشغيل.

## أمثلة

```bash
# تشغيل على منفذ مختلف
failproofai --port 9000

# استخدام مسار مشاريع Claude مخصص عبر متغير البيئة
CLAUDE_PROJECTS_PATH=~/my-claude-projects failproofai
```