---
---
title: "التكاملات والمراجع"
description: "توصيل أجهزة عمل الوكلاء المدعومة و SDK و CLI و HTTP API."
icon: "braces"
---

اختر التكامل الأقرب إلى المكان الذي يعمل فيه وكيلك بالفعل.

<Columns cols={2}>
  <Card title="أجهزة عمل الوكلاء" icon="plug-zap" href="/ar/reference/harnesses">
    ثبّت الخطافات لأدوات سطر أوامر وكلاء الترميز والوكلاء المستقلين المدعومة.
  </Card>
  <Card title="أطر عمل الوكلاء و SDK" icon="plug" href="/ar/start/integrations">
    أداة LangGraph و CrewAI و LlamaIndex و Pydantic AI أو وكيل مخصص.
  </Card>
  <Card title="مرجع Python SDK" icon="python" href="/ar/reference/custom-agents">
    التكوين وكتالوج الأحداث وقواعد الارتباط والتسليم.
  </Card>
  <Card title="لوحة التحكم المحلية" icon="monitor-cog" href="/ar/reference/local-dashboard">
    راجع المشاريع المحلية والجلسات ونشاط السياسة والتدقيق غير المتصل.
  </Card>
  <Card title="Failproof AI CLI" icon="terminal" href="/ar/reference/failproof-cli">
    كوّن الالتقاط المحلي والخطافات والسياسات والتدقيق والتسليم وحالة الجهاز.
  </Card>
  <Card title="Failproof Cloud CLI" icon="cloud-cog" href="/ar/reference/cloud-cli#cli-commands">
    الاستعلام عن جلسات Cloud والتدقيق والمشاكل والتنبيهات والمفاتيح والمستخدمين والإعدادات والإدارة.
  </Card>
  <Card title="Evaluator SDK" icon="gauge" href="/ar/reference/evaluator-sdk">
    تقدير الجلسات المكتملة أو غير النشطة باستخدام خدمة FastAPI.
  </Card>
  <Card title="السياسات المخصصة" icon="shield-plus" href="/ar/reference/policy-sdk">
    قم بتأليف واختبار قرارات allow و instruct و deny خاصة بسير عمل محدد.
  </Card>
  <Card title="الاستضافة الذاتية" icon="cloud-cog" href="/ar/reference/self-hosting">
    انشر مستوى التحكم في Cloud على مجموعة Kubernetes مدارة من قبل العميل.
  </Card>
</Columns>

يغطي مرجع [HTTP API](/ar/reference/http-api) المُنتج سطح `/v1` العام. تشرح الصفحات المكتوبة يدويًا سير العمل الذي يمتد عبر عدة نقاط نهاية أو يستخدم واجهات إدارية خارج هذا السطح العام.

## توصيل وكيل والتحقق من البيانات

<Tabs>
  <Tab title="لوحة التحكم">
    1. افتح **الإدارة → المفاتيح**، أنشئ مفتاحًا باستخدام `events:add` و `policies:pull`، وانسخ السر.
    2. كوّن التكامل باستخدام الصفحة المطابقة أعلاه.
    3. افتح **المراقبة → الأحداث** للتأكد من وصول الأحداث، ثم **المراقبة → الجلسات** للتأكد من تكوينها لتشغيلات كاملة.
    4. صفّي حسب بيئة التكامل وفحّص جلسة واحدة للحصول على حقول النموذج والأداة والخطأ والسياسة اللازمة للتدقيق.

    ابدأ برج المفاتيح. تحدد المنح المحددة ما إذا كان بإمكان الجهاز إرسال الأحداث واستقبال السياسات المدارة من قبل Cloud.

    ![درج مفتاح API الجديد المستخدم لمنح أذونات البتول الأحداث وتسليم السياسة.](/images/dashboard/key-create.png)

    بعد توصيل التكامل، استخدم قائمة الجلسات للتأكد من أن أحداثها يتم تجميعها في عمليات تشغيل كاملة في البيئة المتوقعة.

    ![قائمة الجلسات المستخدمة للتحقق من أن التكامل المتصل حديثًا يبلغ عن عمليات تشغيل وكيل كاملة.](/images/dashboard/sessions-list.png)

    افتح إحدى هذه الجلسات قبل اعتبار التكامل مكتملاً؛ يجب أن يحتوي التتبع على دليل النموذج والأداة والخطأ والسياسة الذي يحتاجه التدقيق.
  </Tab>
  <Tab title="CLI">
    أنشئ مفتاح جهاز، وصّل Failproof daemon، والتحقق من الجلسة الأولى.

    ```bash
    fp keys create agent-production \
      --add events:add \
      --add policies:pull

    failproofai config \
      --connect https://app.befailproof.ai \
      --token <machine-key>

    failproofai flush --wait
    fp sessions --since 1h --env production
    fp events --since 1h --env production --limit 20
    ```

    استخدم `fp --json sessions ...` عندما تستهلك أداة أخرى النتيجة. يجب أن تأتي الأعلام العامة مثل `--json` و `--org` و `--base-url` قبل الأمر.

    اطّلع على [مرجع Failproof AI CLI](/ar/reference/failproof-cli) للأوامر المحلية و [مرجع Failproof Cloud CLI](/ar/reference/cloud-cli#cli-commands) لأوامر `fp`.
  </Tab>
</Tabs>