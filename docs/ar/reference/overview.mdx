---
title: "التكاملات والمراجع"
description: "قم بتوصيل حزم الوكلاء المدعومة وملحقات SDK وواجهات سطر الأوامر والواجهة البرمجية HTTP."
icon: "braces"
---

اختر التكامل الأقرب إلى حيث يعمل وكيلك بالفعل.

<Columns cols={2}>
  <Card title="حزم الوكلاء" icon="plug-zap" href="/ar/reference/harnesses">
    قم بتثبيت الخطاطيف للبرامج المدعومة والوكلاء المستقلين من سطر الأوامر.
  </Card>
  <Card title="وكلاء مخصصون" icon="python" href="/ar/reference/python-sdk">
    قم بأدوات التتبع لإيجاد الأخطاء في الوكلاء المخصصين، ثم اتصل بنا لإضافة الوقاية إلى بيئة التشغيل الخاصة بك.
  </Card>
  <Card title="لوحة المعلومات المحلية" icon="monitor-cog" href="/ar/reference/local-dashboard">
    راجع المشاريع المحلية والجلسات ونشاط السياسات والتدقيق غير المتصل.
  </Card>
  <Card title="Failproof AI CLI" icon="terminal" href="/ar/reference/failproof-cli">
    قم بتكوين الالتقاط المحلي والخطاطيف والسياسات والتدقيق والتسليم وحالة الجهاز.
  </Card>
  <Card title="Failproof Cloud CLI" icon="cloud-cog" href="/ar/reference/cloud-cli#cli-commands">
    الاستعلام والإدارة لجلسات Cloud والتدقيق والمشاكل والتنبيهات والمفاتيح والمستخدمين والإعدادات.
  </Card>
  <Card title="Evaluator SDK" icon="gauge" href="/ar/reference/evaluator-sdk">
    تقييم الجلسات الكاملة أو غير النشطة باستخدام خدمة FastAPI.
  </Card>
  <Card title="السياسات المخصصة" icon="shield-plus" href="/ar/reference/policy-sdk">
    قم بتأليف واختبار قرارات السماح والتعليمات والرفض الخاصة بسير العمل.
  </Card>
  <Card title="الاستضافة الذاتية" icon="cloud-cog" href="/ar/reference/self-hosting">
    قم بنشر لوحة التحكم في Cloud على مجموعة Kubernetes مدارة من قبل العميل.
  </Card>
</Columns>

تغطي [مرجع HTTP API](/ar/reference/http-api) المُنشأ سطح `/v1` العام. تشرح الصفحات المكتوبة يدويًا سير العمل الذي يمتد عبر نقاط نهاية متعددة أو يستخدم واجهات إدارية خارج هذا السطح العام.

## قم بتوصيل وكيل والتحقق من البيانات

<Tabs>
  <Tab title="لوحة المعلومات">
    1. افتح **الإدارة → المفاتيح**، وأنشئ مفتاحًا باستخدام `events:add` و `policies:pull`، وانسخ السر.
    2. قم بتكوين التكامل باستخدام الصفحة المطابقة أعلاه.
    3. افتح **مراقبة → الأحداث** للتأكد من وصول الأحداث، ثم **مراقبة → الجلسات** للتأكد من أنها تشكل عمليات تشغيل كاملة.
    4. قم بالتصفية حسب بيئة التكامل والفحص جلسة واحدة للنموذج والأداة والخطأ وحقول السياسة المطلوبة من قبل عمليات التدقيق.

    ابدأ بدرج المفاتيح. التمنحات المختارة تحدد ما إذا كان الجهاز يمكنه إرسال الأحداث واستقبال السياسات المُدارة من Cloud.

    ![درج مفتاح واجهة برمجية جديد يُستخدم لمنح أذونات إدخال الأحداث وتسليم السياسات.](/images/dashboard/key-create.png)

    بعد توصيل التكامل، استخدم قائمة الجلسات للتأكد من أن أحداثها يتم تجميعها في عمليات تشغيل كاملة في البيئة المتوقعة.

    ![قائمة الجلسات المستخدمة للتحقق من أن التكامل المتصل حديثًا يقرر عمليات تشغيل الوكيل الكاملة.](/images/dashboard/sessions-list.png)

    افتح إحدى هذه الجلسات قبل اعتبار التكامل مكتملًا؛ يجب أن يحتوي التتبع على نموذج وأداة وخطأ وأدلة السياسة التي تحتاجها عمليات التدقيق الخاصة بك.
  </Tab>
  <Tab title="واجهة سطر الأوامر">
    أنشئ مفتاح جهاز، وقم بتوصيل خادم Failproof، والتحقق من الجلسة الأولى.

    ```bash
    fp keys create agent-production \
      --add events:add \
      --add policies:pull

    failproofai config \
      --connect https://app.befailproof.ai \
      --token <machine-key>

    failproofai flush --wait
    fp sessions --since 1h --env production
    fp events --since 1h --env production --limit 20
    ```

    استخدم `fp --json sessions ...` عندما تستهلك أداة أخرى النتيجة. يجب أن تأتي العلامات العامة مثل `--json` و `--org` و `--base-url` قبل الأمر.

    اطلع على [مرجع Failproof AI CLI](/ar/reference/failproof-cli) للأوامر المحلية و [مرجع Failproof Cloud CLI](/ar/reference/cloud-cli#cli-commands) لأوامر `fp`.
  </Tab>
</Tabs>