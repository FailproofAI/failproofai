---
title: "Codex-Sitzungserfassung"
description: "Erfassen Sie die lokalen OpenAI Codex-Sitzungen Ihres Teams in AgentEye als gewöhnliche Sitzungen und Ereignisse – ohne Änderungen an deren bisheriger Arbeitsweise."
---

Ihre Entwickler arbeiten täglich mit OpenAI Codex. Die Codex-Sitzungserfassung bringt diese Coding-Sitzungen als gewöhnliche Sitzungen und Ereignisse in AgentEye, sodass Sie sie durchsuchen, wiedergeben und neben allem anderen, was Sie beobachten, auswerten können. Sie ergänzt das [Python SDK](/de/agenteye/python-sdk): Das SDK instrumentiert Agenten, die Sie selbst schreiben, während diese Funktion die Codex-Arbeit Ihres Teams erfasst – ohne dass sich an deren Arbeitsweise etwas ändert.

Ein kleiner Hintergrundkollektor liest Codex' lokale Sitzungstranskripte, während sie geschrieben werden, und überträgt sie an AgentEye. Ein Kollektor pro Maschine erfasst gleichzeitig alle lokalen Codex-Oberflächen – es ist keine Einrichtung pro Oberfläche erforderlich.

Derselbe Kollektor erfasst auch andere Agenten – siehe [OpenClaw](/de/agenteye/openclaw-capture) und [Hermes](/de/agenteye/hermes-capture). Aktivieren Sie jeden, den Sie verwenden; ein einzelner Kollektor kann mehrere gleichzeitig erfassen.

---

## Was erfasst wird

Jede Codex-Oberfläche, die **lokal** läuft, erzeugt dieselben Sitzungstranskripte auf der Festplatte, und der Kollektor liest sie alle aus:

- die Codex **CLI** und `codex exec`
- die **VS Code / IDE-Erweiterung**
- die **Desktop-App**, wenn sie eine Sitzung lokal ausführt

Jede Codex-Sitzung wird zu einer AgentEye-[Sitzung](/de/agenteye/sessions); ihre Benutzer- und Assistentennachrichten, Überlegungen, Tool-Aufrufe, Tool-Ergebnisse und Token-Nutzung werden zu den entsprechenden [Ereignissen](/de/agenteye/event-stream). Die Oberfläche, von der jede Sitzung stammt (CLI, IDE oder Desktop), wird aufgezeichnet, sodass Sie sie unterscheiden können.

> **Cloud-Sitzungen werden nicht erfasst.** Die Desktop-App führt Sitzungen zunehmend in der Codex-Cloud aus und speichert nur deren Metadaten lokal – es gibt kein lokales Transkript zum Auslesen. Nur lokal ausgeführte Sitzungen werden erfasst.

---

## Aktivierung

Die Erfassung ist deaktiviert, bis Sie sie einschalten. Installieren Sie den Kollektor mit einem API-Schlüssel, der die Berechtigung `events:add` besitzt (siehe [API-Schlüssel](/de/agenteye/api-keys)), und aktivieren Sie die Codex-Erfassung:

```bash
curl -fsSL https://raw.githubusercontent.com/FailproofAI/agenteye-collector/main/install.sh \
  | sh -s -- --key <YOUR_API_KEY> --codex-enabled
```

Dadurch wird der Kollektor installiert, als Hintergrunddienst registriert und die Erfassung gestartet. Überprüfen Sie, ob er läuft:

```bash
agenteye-collector health
```

Beim ersten Start werden Ihre bestehenden Codex-Sitzungen einmalig nachgefüllt, danach wird neue Aktivität innerhalb von Sekunden übertragen. Codex' eigene Dateien werden ausschließlich gelesen – niemals geändert, verschoben oder gelöscht – und jede Sitzung wird genau einmal übertragen, auch nach Neustarts.

---

## Anzeige in AgentEye

Erfasste Sitzungen erscheinen unter **Sessions** und ihre Ereignisse im **Events**-Stream, genau wie jeder andere beobachtete Agent – daher funktionieren [Sitzungswiedergabe](/de/agenteye/sessions), [Suche](/de/agenteye/queries), [Auswertungen](/de/agenteye/evaluations) und [Benachrichtigungen](/de/agenteye/alerts) für sie genauso. Filtern Sie nach dem Codex-Agenten, um nur diese anzuzeigen.

---

## Datenschutz

Codex-Transkripte enthalten die vollständige Sitzung – einschließlich Befehlsausgaben, Dateiinhalten und allem, was Codex gelesen oder geschrieben hat – und können Geheimnisse enthalten. Erfasste Sitzungen werden unverändert übertragen. Aktivieren Sie die Erfassung daher nur auf Maschinen und für Teams, bei denen die Zentralisierung dieser Inhalte in AgentEye angemessen ist, und vergeben Sie an den Kollektor ausschließlich einen auf `events:add` beschränkten Schlüssel. Unter [Sicherheit](/de/agenteye/security) erfahren Sie, wie Ihre Daten isoliert aufbewahrt werden.