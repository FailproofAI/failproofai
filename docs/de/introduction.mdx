---
title: "Failproof AI"
description: "FailproofAI stattet KI-Agenten mit 39 integrierten Fehlerrichtlinien aus, die Schleifen, Secret-Leaks, destruktive Tool-Aufrufe und mehr mit einer einzigen Installation abfangen."
---

[![npm weekly downloads](https://img.shields.io/npm/dw/failproofai?style=flat-square&color=2ea44f)](https://www.npmjs.com/package/failproofai)

Hooks und Richtlinien für **KI-Fehlerbehandlung**, **Fehlerbehebung** und **LLM-Zuverlässigkeit**. Halte deine KI-Agenten zuverlässig und autonom am Laufen – für **Claude Code**, **OpenAI Codex**, **GitHub Copilot**, **Cursor Agent**, **OpenCode**, **Pi**, **Hermes**, **OpenClaw**, **Factory Droid**, **Devin CLI**, **Antigravity CLI** und das **Agents SDK**.

KI-Agenten scheitern auf vorhersehbare Weisen. Sie führen destruktive Befehle aus, leaken Secrets, verlieren den Fokus, stecken in Schleifen fest oder pushen direkt in den Main-Branch. Unbeaufsichtigt können kleine Fehler zu Ausfällen, geleakten Zugangsdaten und verlorenem Arbeitsfortschritt eskalieren.

FailproofAI löst dieses Problem mit **Richtlinien**. Diese Regeln klinken sich in jeden Tool-Aufruf eines Agenten ein, um **Fehler zu erkennen**, **sie zu beheben** (blockieren, anweisen, bereinigen) und **dich zu benachrichtigen**, wenn etwas Aufmerksamkeit erfordert. Ein lokales Dashboard ermöglicht es dir, jeden Tool-Aufruf, jeden Agentenfehler und jede Wiederherstellungsaktion im Nachhinein zu überprüfen.

Es verlassen keine Daten deinen Rechner.

## Loslegen

<CardGroup cols={2}>

<Card title="39 integrierte Richtlinien" icon="shield" href="/de/built-in-policies">
  Destruktive Befehle blockieren, Secret-Leaks verhindern, Agenten innerhalb der Projektgrenzen halten und mehr – alles sofort einsatzbereit.
</Card>

<Card title="Eigene Richtlinien" icon="code" href="/de/custom-policies">
  Schreibe deine eigenen Regeln in JavaScript mit einem einfachen allow / deny / instruct API.
</Card>

<Card title="Agenten-Monitor" icon="chart-line" href="/de/dashboard">
  Sieh, was deine Agenten in deiner Abwesenheit getan haben. Sitzungen durchsuchen, Tool-Aufrufe inspizieren und überprüfen, wo Richtlinien ausgelöst wurden.
</Card>

<Card title="Einfache Konfiguration" icon="gear" href="/de/configuration">
  Jede Richtlinie ohne Code anpassen. Allowlists, geschützte Branches oder Schwellenwerte pro Projekt oder global festlegen.
</Card>

</CardGroup>

## Schnellstart

<CodeGroup>

```bash npm
npm install -g failproofai
```

```bash bun
bun add -g failproofai
```

</CodeGroup>

```bash
failproofai policies --install   # enable policies (or skip — `failproofai` will offer to set them up on first run)
failproofai                      # launch the dashboard
```

Den vollständigen Einstieg findest du im [Erste Schritte](/de/getting-started)-Leitfaden.