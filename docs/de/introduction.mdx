---
title: "Failproof AI"
description: "FailproofAI gibt KI-Agenten 39 integrierte Fehler-Policies, die Schleifen, geheime Datenlecks, destruktive Tool-Aufrufe und mehr mit einer einzigen Installation abfangen."
---

[![npm weekly downloads](https://img.shields.io/npm/dw/failproofai?style=flat-square&color=2ea44f)](https://www.npmjs.com/package/failproofai)

Hooks und Policies für **KI-Fehlerbehandlung**, **Fehlerwiederherstellung** und **LLM-Zuverlässigkeit**. Halten Sie Ihre KI-Agenten zuverlässig und autonom am Laufen – mit **Claude Code**, **OpenAI Codex**, **GitHub Copilot**, **Cursor Agent**, **OpenCode**, **Pi**, **Hermes**, **OpenClaw**, **Factory Droid**, **Devin CLI**, **Antigravity CLI** und dem **Agents SDK**.

KI-Agenten scheitern auf vorhersehbare Weise. Sie führen destruktive Befehle aus, geben Geheimnisse preis, driften von der Aufgabe ab, geraten in Schleifen oder pushen direkt in den Main-Branch. Unbeaufsichtigt können kleine Fehler zu Ausfällen, durchgesickerten Zugangsdaten und verlorenem Work eskalieren.

FailproofAI löst dieses Problem mit **Policies**. Diese Regeln greifen bei jedem Tool-Aufruf eines Agenten ein, um **Fehler zu erkennen**, **sie zu beheben** (blockieren, anweisen, bereinigen) und **Sie zu benachrichtigen**, wenn etwas Aufmerksamkeit erfordert. Ein lokales Dashboard ermöglicht es Ihnen, jeden Tool-Aufruf, jeden Agentenfehler und jede Wiederherstellungsmaßnahme im Nachhinein zu überprüfen.

Transkripte und Policy-Auswertungen bleiben auf Ihrem Gerät. Daten werden nur dann übertragen, wenn Sie ausdrücklich eine Online-Funktion nutzen, etwa authentifizierte Audit-Erinnerungen oder Einladungen.

## Erste Schritte

<CardGroup cols={2}>

<Card title="39 integrierte Policies" icon="shield" href="/de/built-in-policies">
  Destruktive Befehle blockieren, geheime Datenlecks verhindern, Agenten innerhalb von Projektgrenzen halten und vieles mehr – alles direkt einsatzbereit.
</Card>

<Card title="Eigene Policies" icon="code" href="/de/custom-policies">
  Schreiben Sie Ihre eigenen Regeln in JavaScript mit einer einfachen allow / deny / instruct API.
</Card>

<Card title="Agent-Monitor" icon="chart-line" href="/de/dashboard">
  Sehen Sie, was Ihre Agenten in Ihrer Abwesenheit getan haben. Sitzungen durchsuchen, Tool-Aufrufe inspizieren, nachverfolgen, wo Policies ausgelöst wurden.
</Card>

<Card title="Einfache Konfiguration" icon="gear" href="/de/configuration">
  Jede Policy ohne Code anpassen. Allowlists, geschützte Branches oder Schwellenwerte pro Projekt oder global festlegen.
</Card>

</CardGroup>

## Schnellstart

<CodeGroup>

```bash npm
npm install -g failproofai
```

```bash bun
bun add -g failproofai
```

</CodeGroup>

```bash
failproofai policies --install   # enable policies (or skip — `failproofai` will offer to set them up on first run)
failproofai                      # launch the dashboard
```

Eine vollständige Anleitung finden Sie im [Erste-Schritte-Leitfaden](/de/getting-started).