---
title: Auditar sesiones pasadas (beta)
description: "Cuenta con qué frecuencia el agente realizó acciones costosas o arriesgadas en transcripciones anteriores"
---

<Note>
  **Función beta.** La auditoría se lanza en beta mientras recopilamos comentarios tempranos.
  El catálogo de detectores y el formato del informe pueden cambiar antes del próximo corte estable.
  Por favor, abre un issue si algo parece incorrecto.
</Note>

La auditoría ahora está disponible como la **página del panel /audit**, no como un subcomando de la CLI. Ábrela desde la barra de navegación del panel (entre Policies y Projects), o visita `http://localhost:8020/audit` directamente cuando ejecutes `failproofai` de forma local.

```bash
failproofai          # abre el panel y haz clic en "Audit"
```

El panel analiza las transcripciones pasadas de la CLI del agente en esta máquina (Claude Code, Codex, Copilot, Cursor, OpenCode, Pi, Gemini) e informa con qué frecuencia el agente realizó acciones que failproofai está diseñado para detener: comprobaciones de variables de entorno, force pushes, prefijos redundantes `cd <cwd>`, bucles de sleep-polling, releer archivos recién editados, y más.

Para cada transcripción, cada evento de uso de herramienta se reproduce a través de las 39 políticas integradas **y** de 8 detectores exclusivos de auditoría que capturan patrones aún no cubiertos por las políticas en tiempo de ejecución. Los recuentos se agregan por política/detector en todas las sesiones.

## Qué obtienes

La página `/audit` contiene seis secciones:

1. **Identidad** — tu agente clasificado en uno de 8 arquetipos (`optimist`, `cowboy`, `explorer`, `goldfish`, `paranoid architect`, `precision builder`, `hammer`, `ghost`) basado en la señal ponderada de todas las transcripciones auditadas.
2. **Fortalezas** — cifras reales derivadas del análisis (% de llamadas limpias, "0 filtraciones de credenciales", etc.) condicionadas a que las políticas de sanitización relevantes hayan disparado realmente.
3. **Puntuación** — de 0 a 100 con bandas S/A/B/C/D/F y una proyección de mejora si se habilitaran todas las políticas recomendadas.
4. **Hallazgos** — tarjetas por política con lo que ocurrió, el coste, las evidencias capturadas y el comando exacto `failproofai policy add <slug>` para habilitar la función integrada en tiempo real que lo habría detectado.
5. **Políticas recomendadas** — lista de instalación consolidada con un comando único `failproofai policies --install`.
6. **Recordatorio de nueva auditoría** — "vuelve mejor". Configura un recordatorio por correo electrónico cada 7 días a través del servidor de API (requiere inicio de sesión; consulta [`failproofai auth`](/es/cli/auth)).

## Detectores exclusivos de auditoría

Estos detectan patrones de "comportamiento ineficiente" que aún no se aplican en tiempo real. Solo se ejecutan durante la auditoría y nunca bloquean una llamada de herramienta en vivo.

| Detector | Qué cuenta |
|---|---|
| `redundant-cd-cwd` | Comandos Bash que comienzan con `cd <cwd> && …` aunque los comandos ya se ejecutan en `cwd`. |
| `prefer-edit-over-read-cat` | `cat`/`head`/`tail`/`less`/`more` sobre un único archivo fuente — usa la herramienta `Read`. |
| `prefer-edit-over-sed-awk` | Ediciones en sitio con `sed -i` / `awk … > file` — usa la herramienta `Edit`. |
| `prefer-write-over-heredoc` | Escritura de archivos mediante heredoc o `echo > file` multilínea — usa la herramienta `Write`. |
| `sleep-polling-loop` | `sleep N` largo (≥ 30s) o bucles de polling con `while …; sleep …; done`. |
| `find-from-root` | `find /`, `find /home`, `find /usr`, etc. — limita el alcance a `cwd`. |
| `git-commit-no-verify` | `git commit … --no-verify` / `-n`, omitiendo los hooks. |
| `reread-after-edit` | `Read` de un archivo que acaba de ser editado con `Edit`/`Write` en la misma sesión. |

## Cachés

- **Caché por transcripción** en `~/.failproofai/cache/audit/<sha1>.json` indexada por `(mtime, size, engineVersion, detectorVersion)` — se invalida automáticamente cuando cambia la transcripción o el código de políticas/detectores. Cada entrada también almacena un timestamp `cachedAt` como **metadato TTL** (no forma parte de la clave de caché); las entradas con más de **7 días** se rechazan en la lectura para que los resultados de larga duración no sobrevivan a la evolución de la intención de los detectores.
- **Caché del resultado completo** en `~/.failproofai/audit-dashboard.json` (modo 0600). Permite que el panel se renderice instantáneamente al navegar sin necesidad de volver a ejecutar el análisis. También se rechaza al leer si supera el **TTL de 7 días** — `/audit` entonces cae a su estado vacío y solicita una ejecución nueva. Haz clic en `[ re-audit now ]` cerca de la parte inferior del informe para actualizar; la nueva ejecución transmite el progreso mediante una franja fija en la parte superior e intercambia el resultado en tiempo real al completarse con éxito (sin recarga de página).

## Notas

- **Sin mutaciones.** La auditoría se reproduce en modo de solo lectura. `warn-repeated-tool-calls` se omite porque de lo contrario su archivo sidecar por sesión sería modificado.
- **Políticas de flujo de trabajo omitidas.** Las políticas `require-*-before-stop` solo se activan en eventos `Stop` y ejecutan `execSync` contra el estado git en vivo — no tienen una interpretación significativa de "qué habría pasado en 2025", por lo que no aparecen en los recuentos de auditoría.
- **Políticas personalizadas omitidas.** Los hooks personalizados proporcionados por el usuario no se reproducen (pueden haber cambiado desde la sesión original).