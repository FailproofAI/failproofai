---
title: "Failproof AI"
description: "FailproofAI proporciona a los agentes de IA 39 políticas de fallos integradas que detectan bucles, fugas de secretos, llamadas a herramientas destructivas y más en una sola instalación."
---

[![npm weekly downloads](https://img.shields.io/npm/dw/failproofai?style=flat-square&color=2ea44f)](https://www.npmjs.com/package/failproofai)

Hooks y políticas para el **manejo de fallos de IA**, **recuperación de errores** y **fiabilidad de LLM**. Mantén tus agentes de IA confiables y funcionando de forma autónoma en **Claude Code**, **OpenAI Codex**, **GitHub Copilot**, **Cursor Agent**, **OpenCode**, **Pi**, **Hermes**, **OpenClaw**, **Factory Droid**, **Devin CLI**, **Antigravity CLI** y el **Agents SDK**.

Los agentes de IA fallan de maneras predecibles. Ejecutan comandos destructivos, filtran secretos, se desvían de sus tareas, quedan atrapados en bucles o hacen push directamente a main. Sin supervisión, los pequeños fallos se encadenan en interrupciones, credenciales filtradas y trabajo perdido.

FailproofAI resuelve esto con **políticas**. Estas reglas se enganchan en cada llamada a herramienta del agente para **detectar fallos**, **mitigarlos** (bloquear, instruir, sanitizar) y **alertarte** cuando algo requiere atención. Un panel de control local te permite revisar cada llamada a herramienta, fallo del agente y acción de recuperación después del hecho.

Las transcripciones y la evaluación de políticas permanecen en tu máquina. Los datos solo se envían cuando utilizas explícitamente una función en línea, como recordatorios de auditoría autenticados o invitaciones.

## Primeros pasos

<CardGroup cols={2}>

<Card title="39 políticas integradas" icon="shield" href="/es/built-in-policies">
  Bloquea comandos destructivos, previene fugas de secretos, mantiene a los agentes dentro de los límites del proyecto y más. Todo listo desde el primer momento.
</Card>

<Card title="Políticas personalizadas" icon="code" href="/es/custom-policies">
  Escribe tus propias reglas en JavaScript con una API sencilla de allow / deny / instruct.
</Card>

<Card title="Monitor de agentes" icon="chart-line" href="/es/dashboard">
  Descubre qué hicieron tus agentes mientras no estabas. Navega por sesiones, inspecciona llamadas a herramientas y revisa dónde se activaron las políticas.
</Card>

<Card title="Configuración sencilla" icon="gear" href="/es/configuration">
  Ajusta cualquier política sin código. Define listas de permitidos, ramas protegidas o umbrales por proyecto o de forma global.
</Card>

</CardGroup>

## Inicio rápido

<CodeGroup>

```bash npm
npm install -g failproofai
```

```bash bun
bun add -g failproofai
```

</CodeGroup>

```bash
failproofai policies --install   # enable policies (or skip — `failproofai` will offer to set them up on first run)
failproofai                      # launch the dashboard
```

Consulta la guía de [Primeros pasos](/es/getting-started) para ver el recorrido completo.