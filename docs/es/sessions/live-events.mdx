---
title: "Eventos en vivo"
description: "Observa la actividad del agente mientras una sesión está en ejecución."
icon: "radio"
---

Los eventos en vivo te permiten confirmar la instrumentación y supervisar una ejecución de riesgo sin tener que esperar a que la sesión termine.

## Ver actividad

<Tabs>
  <Tab title="Dashboard">
    1. Ve a **Observe → Events**.
    2. Comienza con la ventana de tiempo actual y sin filtros para confirmar que los datos están llegando.
    3. Filtra por entorno, tipo de evento, agente o sesión. Usa la búsqueda para texto del payload.
    4. Selecciona un evento para inspeccionar su resumen y detalles. Sigue el enlace de sesión para ver el trazado completo.

    ![El flujo de eventos en vivo mostrando tráfico de un gateway de chat Hermes junto a ejecuciones de coding-CLI, filtrado por tiempo, entorno, tipo de evento, agente, modelo, contexto y sesión.](/images/dashboard/events-stream-current.png)
  </Tab>
  <Tab title="CLI">
    ```bash
    fp events --env production --event-type tool_use,error --limit 100
    fp events --session-id <session-id> --order asc --all
    fp --json events --full --session-id <session-id> --all
    ```

    El feed por defecto omite los payloads sin procesar. Usa `--full` solo para una investigación acotada de sesión.
  </Tab>
</Tabs>

Usa el flujo de eventos para responder tres preguntas inmediatas:

- ¿El agente esperado está reportando al entorno correcto?
- ¿Las llamadas al modelo, las llamadas a herramientas y las decisiones de política están llegando en orden?
- ¿La sesión ha dejado de avanzar o ha comenzado a repetir una acción?

Los tipos de eventos incluyen eventos del ciclo de vida del agente, solicitudes y respuestas del modelo, uso de herramientas y resultados, ejecución de hooks, esperas e interrupciones humanas, y errores explícitos. Los IDs de correlación vinculan eventos emparejados, como una llamada a herramienta y su resultado.

<Tip>
  Mantén la vista en vivo con filtros amplios al verificar una nueva integración. Agrega filtros solo después de ver el primer evento; un filtro incorrecto puede parecer un fallo de ingesta.
</Tip>

Si no aparece ningún evento, ejecuta `failproofai config --status` y luego [soluciona problemas de ingesta](/es/reference/troubleshooting).

Los agentes que no se ejecutan en uno de los 12 [harnesses](/es/reference/harnesses) compatibles reportan los mismos tipos de eventos a través del [SDK de Python](/es/reference/custom-agents), incluidos los eventos de human-in-the-loop (`human_wait`, `human_input`, `human_interrupt`) de los que dependen los agentes de gateway y producción.