---
---
title: התקנת מדיניות
description: "הפעל מדיניות כך שהן יפעלו בכל קריאת כלי של אג'נט"
---

```bash
failproofai policies --install [policy-names...] [options]
```

כותב רשומות hook לקובץ ההגדרות של ה-CLI של האג'נט המותקן שלך (Claude Code, OpenAI Codex, או GitHub Copilot CLI _(beta)_) כדי שה-failproofai יחתוך קריאות כלים.

כינויים: `failproofai p -i`

## אפשרויות

| דגל | תיאור |
|------|-------------|
| `--cli claude\|codex\|copilot` | ה-CLI של האג'נט (או קליים) להתקנה; מופרדים ברווח (למשל `--cli claude codex copilot`) או חוזרים. השמט כדי לגלות CLIs מותקנות ותבקש. |
| `--scope user` | התקן לקובץ הגדרות בהיקף משתמש (Claude: `~/.claude/settings.json`; Codex: `~/.codex/hooks.json`; Copilot: `~/.copilot/hooks/failproofai.json`). ברירת מחדל. |
| `--scope project` | התקן לקובץ הגדרות בהיקף פרויקט (Claude: `<cwd>/.claude/settings.json`; Codex: `<cwd>/.codex/hooks.json`; Copilot: `<cwd>/.github/hooks/failproofai.json`). |
| `--scope local` | Claude בלבד — מתקין ל-`<cwd>/.claude/settings.local.json`. ל-Codex וה-Copilot אין היקף `local`. |
| `--custom <path>` / `-c` | נתיב לקובץ JS המכיל מדיניות hook מותאמות אישית |

## התנהגות

- **ללא שמות מדיניות** - פותח הנמקה אינטראקטיבית לבחירת מדיניות
- **שמות ספציפיים** - מאפשר אותן מדיניות (מוסיף לכל המדיניות שכבר מופעלות)
- **`all`** - מאפשר כל מדיניות זמינה

ההתקנה היא תוסף: הפעלת `--install` שוב מוסיפה מדיניות חדשה ללא הסרת קיימות.

## דוגמאות

```bash
# התקן את כל המדיניות הברירות המחדל בעולם (אינטראקטיבי)
failproofai policies --install

# התקן מדיניות ספציפיות לפרויקט הנוכחי
failproofai policies --install block-sudo sanitize-api-keys --scope project

# הפעל את כל המדיניות בבת אחת
failproofai policies --install all

# התקן עם קובץ מדיניות מותאם אישית
failproofai policies --install --custom ./my-policies.js

# התקן עבור OpenAI Codex (היקף פרויקט)
failproofai policies --install --cli codex --scope project

# התקן עבור GitHub Copilot CLI (beta) לפרויקט הנוכחי
failproofai policies --install --cli copilot --scope project

# התקן עבור כל שלושת ה-CLIs בבת אחת
failproofai policies --install --cli claude codex copilot
```

כאשר `--custom <path>` מסופק, הקובץ מולבן בעצם הרגע - הוא חייב לקרוא ל-`customPolicies.add()` לפחות פעם אחת. הנתיב המפורש נשמר ב-`policies-config.json` כ-`customPoliciesPath`.