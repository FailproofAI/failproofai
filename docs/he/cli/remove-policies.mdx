---
---
title: הסרת מדיניות
description: "הסרת רשומות hook מהגדרות Claude Code"
---

```bash
failproofai policies --uninstall [policy-names...] [options]
```

מסיר כניסות failproofai hook מ־`settings.json` של Claude Code.

כינויים: `failproofai p -u`

## אפשרויות

| דגל | תיאור |
|------|-------------|
| `--scope user` | הסרה מהגדרות גלובליות (ברירת מחדל) |
| `--scope project` | הסרה מהגדרות הפרויקט |
| `--scope local` | הסרה מהגדרות מקומיות |
| `--scope all` | הסרה מכל ההיקפים בבת אחת |
| `--custom` / `-c` | ניקוי של `customPoliciesPath` מההגדרות |

## התנהגות

- **ללא שמות מדיניות** - מסיר את כל כניסות failproofai hook מקובץ ההגדרות
- **שמות ספציפיים** - משבית את המדיניויות הללו אך שומר על ה־hooks מותקנים

## דוגמאות

```bash
# הסרת כל ה־hooks בגלובלי
failproofai policies --uninstall

# השבית מדיניות ספציפית (שומר על hooks מותקנים)
failproofai policies --uninstall block-sudo

# הסרת hooks מכל היקף
failproofai policies --uninstall --scope all

# ניקוי נתיב המדיניויות המותאמות
failproofai policies --uninstall --custom
```