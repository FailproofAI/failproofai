---
title: "אינטגרציות והפניות"
description: "חבר מחסנים נתמכים של סוכנים, SDKs, CLIs, וה-HTTP API."
icon: "braces"
---

בחר את האינטגרציה הקרובה ביותר למקום שבו הסוכן שלך כבר פועל.

<Columns cols={2}>
  <Card title="מחסנים של סוכנים" icon="plug-zap" href="/he/reference/harnesses">
    התקן hooks עבור CLIs נתמכים של סוכנים מקודדים וקודמים.
  </Card>
  <Card title="סוכנים frameworks ו-SDKs" icon="plug" href="/he/start/integrations">
    סדר LangGraph, CrewAI, LlamaIndex, Pydantic AI, או סוכן מותאם אישית.
  </Card>
  <Card title="Python SDK reference" icon="python" href="/he/reference/custom-agents">
    תצורה, קטלוג האירועים, כללי מתאם, והעברה.
  </Card>
  <Card title="לוח בקרה מקומי" icon="monitor-cog" href="/he/reference/local-dashboard">
    סקור פרויקטים מקומיים, סשנים, פעילות מדיניות, וביקורות עצמאיות.
  </Card>
  <Card title="Failproof AI CLI" icon="terminal" href="/he/reference/failproof-cli">
    קבע תצורה של כיבוד מקומי, hooks, מדיניויות, ביקורות, העברה, וסטטוס מכונה.
  </Card>
  <Card title="Failproof Cloud CLI" icon="cloud-cog" href="/he/reference/cloud-cli#cli-commands">
    שאל וניהול סשנים בענן, ביקורות, בעיות, התראות, מפתחות, משתמשים והגדרות.
  </Card>
  <Card title="Evaluator SDK" icon="gauge" href="/he/reference/evaluator-sdk">
    דרג סשנים מלאים או לא פעילים עם שירות FastAPI.
  </Card>
  <Card title="מדיניויות מותאמות אישית" icon="shield-plus" href="/he/reference/policy-sdk">
    תן וביקורת החלטות ספציפיות לזרימת עבודה של allow, instruct, ו-deny.
  </Card>
  <Card title="Self-hosting" icon="cloud-cog" href="/he/reference/self-hosting">
    פרוס את מישור הבקרה של Cloud בקלסטר Kubernetes שמנוהל על ידי הלקוח.
  </Card>
</Columns>

ההפניה [HTTP API](/he/reference/http-api) שנוצרה מכסה את משטח `/v1` הציבורי. עמודים שנכתבו ביד מסבירים זרימות עבודה החוצות נקודות קצה מרובות או משתמשות בממשקי ניהול מחוץ למשטח הציבורי הזה.

## חבר סוכן וודא נתונים

<Tabs>
  <Tab title="לוח בקרה">
    1. פתח **Administration → Keys**, צור מפתח עם `events:add` ו-`policies:pull`, והעתק את הסוד.
    2. קבע תצורה של האינטגרציה באמצעות העמוד המתאים למעלה.
    3. פתח **Observe → Events** כדי לאשר שאירועים מגיעים, ולאחר מכן **Observe → Sessions** כדי לאשר שהם יוצרים הפעלות מלאות.
    4. סנן לסביבת האינטגרציה ובדוק סשן אחד כדי להבחין בשדות המודל, הכלי, השגיאה והמדיניות הדרושים לביקורות.

    התחל עם מגירת המפתחות. ההרשאות שנבחרו קובעות אם המכונה יכולה לשלוח אירועים וקבל מדיניויות הנוהלות בענן.

    ![מגירת המפתח החדש של ה-API המשמשת להענקת הרשאות בליעת אירועים והעברת מדיניות.](/images/dashboard/key-create.png)

    לאחר חיבור האינטגרציה, השתמש ברשימת הסשנים כדי לאשר שהאירועים שלה מקובצים להפעלות מלאות בסביבה הצפויה.

    ![רשימת הסשנים המשמשת לאימות שאינטגרציה שחוברה לאחרונה מדווחת על הפעלות סוכנים מלאות.](/images/dashboard/sessions-list.png)

    פתח את אחד מהסשנים הללו לפני שאתה מיישם את האינטגרציה; העקבות צריכה להכיל את המודל, הכלי, השגיאה וראיות המדיניות שהביקורות שלך צריכות.
  </Tab>
  <Tab title="CLI">
    צור מפתח מכונה, חבר את ה-Failproof daemon, וודא את הסשן הראשון.

    ```bash
    fp keys create agent-production \
      --add events:add \
      --add policies:pull

    failproofai config \
      --connect https://app.befailproof.ai \
      --token <machine-key>

    failproofai flush --wait
    fp sessions --since 1h --env production
    fp events --since 1h --env production --limit 20
    ```

    השתמש ב-`fp --json sessions ...` כאשר כלי אחרת תצרוך את התוצאה. דגלים גלובליים כגון `--json`, `--org`, ו-`--base-url` חייבים להיות לפני הפקודה.

    ראה את [Failproof AI CLI reference](/he/reference/failproof-cli) לפקודות מקומיות ו-[Failproof Cloud CLI reference](/he/reference/cloud-cli#cli-commands) עבור פקודות `fp`.
  </Tab>
</Tabs>