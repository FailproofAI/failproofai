---
title: "एजेंट के लिए CLI रेसिपीज़"
description: "कॉपी-पेस्ट क्वेरी पैटर्न और jq रेसिपीज़ जो सेशन, इवेंट और मूल्यांकन डेटा को कुछ ऐसा बनाती हैं जो स्क्रिप्ट या कोडिंग एजेंट स्वचालित कर सकते हैं।"
---

सेशन, इवेंट और मूल्यांकन डेटा को सीधे स्क्रिप्ट या कोडिंग एजेंट से खींचें (और पुनः-मूल्यांकन को ट्रिगर करें), साफ JSON के साथ stdout पर जो सीधे `jq` में पाइप होता है। ये रेसिपीज़ failproofai Observability के डेटा को कुछ ऐसा बनाती हैं जो टर्मिनल उपयोगकर्ता या AI कोडिंग एजेंट (Claude Code, Cursor) डैशबोर्ड के माध्यम से क्लिक किए बिना क्वेरी और स्वचालित कर सकते हैं।

नीचे दिए गए पैटर्न failproofai Observability CLI (`agenteye`) के लिए कॉपी-पेस्ट के लिए तैयार हैं। इंस्टॉलेशन, प्रमाणीकरण और पूर्ण विकल्प सूची के लिए [CLI](/hi/agenteye/cli) देखें; अंतर्निर्मित सहायता के लिए `agenteye -h` या `agenteye <command> -h` चलाएँ।

## सुनहरे नियम

1. **वैश्विक विकल्प कमांड से *पहले* जाते हैं।** `agenteye --json sessions` सही है; `agenteye sessions --json` नहीं है। वैश्विक विकल्प हैं `--json`, `--base-url`, `--org`, `--token`, `--insecure`/`--secure`, `--timeout`, `--quiet`, `--no-color`।
2. **जब भी आप आउटपुट पार्स करें तो `--json` पास करें।** डेटा **stdout** पर JSON के रूप में जाता है; मानव स्थिति और त्रुटियां stderr पर जाती हैं, इसलिए stdout `jq` में पाइप करने के लिए साफ रहता है।
3. **stderr टेक्स्ट पर नहीं, exit कोड पर शाखा करें**: `0` ठीक है · `1` अप्रत्याशित त्रुटि · `2` खराब तर्क · `3` डैशबोर्ड तक नहीं पहुंच सकते · `4` लॉगिन नहीं है या समाप्त · `5` अनुमति नहीं है · `6` संसाधन नहीं मिला।
4. **`-h` से खोजें।** हर कमांड अपने फ़िल्टर, मान प्रारूप और JSON आकार को दस्तावेज़ित करता है।

## एक बार सेटअप

```bash
export AGENTEYE_DASHBOARD_URL=https://agenteye.example.com   # ताकि आपको --base-url दोहराना न पड़े
agenteye login --email you@example.com                       # ईमेल किया गया कोड पेस्ट करें; ~24h वैध
```

## काम करने से पहले प्रमाणीकरण की पुष्टि करें

`whoami` कभी भी लापता या समाप्त सेशन पर त्रुटि नहीं देता; यह इसके बजाय `logged_in:false` रिपोर्ट करता है, इसलिए एजेंट सुरक्षित रूप से प्रमाणीकरण स्थिति को जांच सकता है। (यदि कोई बेस URL सेट नहीं है या डैशबोर्ड अप्राप्य है तो यह अभी भी गैर-शून्य exit कर सकता है।)

```bash
if [ "$(agenteye --json whoami | jq -r .logged_in)" != "true" ]; then
  echo "Not authenticated. Run: agenteye login" >&2; exit 1
fi
```

## विफल या कम-स्कोरिंग सेशन खोजें

```bash
# पिछले 24h में सेशन जिनका मूल्यांकन त्रुटि करता है
agenteye --json sessions --since 24h --status error | jq -r '.sessions[].session_id'

# एक एजेंट के लिए सहायकता पर <= 0.5 स्कोर करने वाले मूल्यांकन
agenteye --json evals --agent-id checkout-bot --score helpfulness:..0.5 \
  | jq '.evaluations[] | {session_id, scores}'
```

स्कोर फ़िल्टरिंग **`evals`** पर रहती है, `sessions` पर नहीं। `--score KEY:MIN..MAX` दोहराई जाने योग्य है और AND-संयुक्त है; किसी भी सीमा को वैकल्पिक किया जा सकता है (`..0.5` मतलब ≤ 0.5, `0.9..` मतलब ≥ 0.9)। आप प्रति अनुरोध 20 स्कोर फ़िल्टर तक पास कर सकते हैं; अधिक HTTP 400 लौटाता है। `sessions` `--env`, `--status`, `--agent-id`, `--session-id` और `evals` के साथ समय-सीमा फ़िल्टर साझा करता है, लेकिन कोई `--score` नहीं है।

## एक सेशन अंत से अंत तक पढ़ें

कोई एकल `session show` कमांड नहीं है। इवेंट ट्रेल को सेशन के मूल्यांकन के साथ संयोजित करें:

```bash
# सेशन का नवीनतम मूल्यांकन (स्थिति + स्कोर)
agenteye --json evals --session-id run-001 | jq '.evaluations[0] | {status, scores}'

# रन में हर इवेंट (पूर्ण स्वीप के लिए --limit बढ़ाएँ)
agenteye --json events --session-id run-001 --all --limit 1000 | jq '.events[] | {ts, event_type}'

# सेशन में सिर्फ टूल कॉल (कच्चे पेलोड पाने के लिए --full आवश्यक है)
agenteye --json events --full --session-id run-001 --event-type tool_use,tool_result --all \
  | jq '.events[].payload'
```

> **नोट:** डिफ़ॉल्ट रूप से, `events` तेज़, पेलोड-मुक्त फ़ीड पढ़ता है। प्रत्येक इवेंट में सर्वर-गणना की गई एक-पंक्ति `summary` और `is_error` और टोकन गणना जैसे फ़्लैग होते हैं, लेकिन `payload` `{}` के रूप में वापस आता है। कच्चे पेलोड को खींचने के लिए, `--full` (या `--fields payload`) जोड़ें। पूर्ण फ़ीड पैमाने पर धीमा है, इसलिए इसे सीमित रखें: `--full` को एकल `--session-id` के साथ जोड़ी।

## सब कुछ प्राप्त करें (पृष्ठांकन)

परिणाम सबसे नए-पहले हैं और कर्सर-पैजिनेटेड हैं।

```bash
# एक शॉट: 200-पंक्ति पृष्ठों में 500 पंक्तियों तक प्राप्त करें
agenteye --json events --session-id run-001 --limit 500 --all > events.json

# मैनुअल पृष्ठांकन: अगले कर्सर को वापस फ़ीड करें
page=$(agenteye --json events --limit 100)
cursor=$(echo "$page" | jq -r '.next_cursor // empty')
[ -n "$cursor" ] && agenteye --json events --limit 100 --cursor "$cursor"
```

## --fields के साथ आउटपुट को कम करें

कीज़ को (तालिका और `--json` दोनों में) प्रतिबंधित करें ताकि एजेंट को क्या पढ़ना है यह कम हो।

```bash
agenteye --json sessions --since 7d --fields session_id,status,scores | jq -c '.sessions[]'
agenteye --json events --session-id run-001 --fields ts,event_type --all
```

अज्ञात फील्ड नाम को अस्वीकार किया जाता है (exit `2`) वैध सूची के साथ, फील्ड नाम खोजने का एक सस्ता तरीका।

## वैध फ़िल्टर मान खोजें

```bash
agenteye --json list envs | jq -r '.values[]'           # --env के लिए मान
agenteye --json list tools | jq -r '.values[]'          # टूल नाम; साथ ही agents, models, event_types, ...
agenteye --json list score_filters | jq -r '.values[]'  # --score KEY:MIN..MAX के लिए वैध KEY
```

## अपना org चुनें (मल्टी-टेनेंट)

यदि आप एक से अधिक org से संबंधित हैं, तो लॉगिन पर सक्रिय टेनेंट चुनें (यह सहेजा जाता है):

```bash
agenteye login --org acme --email you@corp.com   # लॉगिन के समान चरण में टेनेंट सेट करें
agenteye --json orgs list | jq -r '.orgs[].org_slug'
agenteye --org globex --json sessions --since 24h   # एक कमांड के लिए ओवरराइड करें
```

`--org` के बिना मल्टी-org लॉगिन गैर-शून्य exit करता है और चुनने के लिए orgs प्रिंट करता है।

## SDK/कलेक्टर के लिए API कुंजी प्रदान करें

```bash
# गोपनीयता एक बार प्रिंट की जाती है, --json के साथ यह .key फील्ड है
key=$(agenteye --json keys create ci-bot --add events:read.add | jq -r '.key')
agenteye keys regenerate ci-bot --yes    # घुमाएँ; निष्क्रिय करने के लिए agenteye keys disable ci-bot --yes
```

## सहेजी गई या तदर्थ क्वेरी चलाएँ

```bash
agenteye --json query run --sql "select count(*) from analytics.events" | jq '.rows'
agenteye --json query run errs --arg prod | jq '.rows'   # एक सहेजी गई क्वेरी + एक स्थितीय $1
```

## इंसिडेंट को गैर-इंटरैक्टिवली ट्रिएज करें

```bash
id=$(agenteye --json incidents list --state firing | jq -r '.incidents[0].id')
agenteye incidents ack "$id"
agenteye incidents assign "$id" --assignee you@corp.com
agenteye incidents resolve "$id" --yes
```

> **नोट:** म्यूटेशन `--json` के अंतर्गत अपनी पुष्टि प्रॉम्प्ट को स्वचालित रूप से छोड़ देते हैं या जब stdin TTY नहीं है, इसलिए एजेंट कभी हैंग नहीं होते; `--yes`/`-y` पास करें यह अन्यत्र स्पष्ट रूप से छोड़ने के लिए।

## स्क्रिप्ट में exit-कोड हैंडलिंग

```bash
out=$(agenteye --json sessions --since 1h) || code=$?
case "${code:-0}" in
  0) echo "$out" | jq '.sessions | length' ;;
  4) echo "Session expired - run 'agenteye login'." >&2 ;;
  5) echo "Missing permission (ask an admin for evaluations:read)." >&2 ;;
  3) echo "Dashboard unreachable - check the URL." >&2 ;;
  *) echo "Unexpected error (exit ${code})." >&2 ;;
esac
```

## JSON आउटपुट आकार

| कमांड | stdout JSON (`--json` के साथ) |
|---|---|
| `whoami` | `{"logged_in": true, "id", "email", "is_instance_admin", "active_org", "permissions": [...], "memberships": [...]}` या `{"logged_in": false}` |
| `orgs list` | `{"active_org", "orgs": [{"org_slug","org_name","permission_set","permissions"}]}` |
| `events` | `{"events": [...], "next_cursor": <cursor or null>}` |
| `evals` | `{"evaluations": [...], "next_cursor": <cursor or null>}` |
| `sessions` | `{"sessions": [...], "next_cursor": <cursor or null>}` |
| `errors` | `{"errors": [...], "next_cursor": <cursor or null>}` |
| `list <kind>` | `{"kind", "values": [...]}` |
| `keys list` / `keys create` | `{"keys": [...]}` / `{id, name, permissions, created_at, key}` (`key` एक बार दिखाया गया) |
| `query run` | `{columns: [{name,type}], rows: [[...]], truncated, elapsed_ms}` |
| `users list` / `settings list` | `{"users": [...]}` / `{"settings": [...]}` |
| `alerts list` / `incidents list` | `{"alerts": [...]}` / `{"incidents": [...]}` |
| बनाएँ/अपडेट/हटाएँ (कोई भी) | संसाधन ऑब्जेक्ट, या डिलीट के लिए `{"deleted": true, "id"}` |
| विफलता (कोई भी, `--json` के साथ) | `{"error": "...", "exit_code": <n>, "status"?: <http>, "hint"?: "..."}` stdout पर |

- प्रत्येक **event** आइटम (`events`): `id, session_id, agent_id, event_type, ts, payload, environment, summary, is_error, error_type, output_tokens, context_window, context_fill`। ध्यान दें कि `payload` `{}` है जब तक आप `--full` (या `--fields payload`) के साथ पूर्ण फ़ीड का अनुरोध नहीं करते।
- प्रत्येक **evaluation** आइटम (`evals`): `id, session_id, agent_id, environment, status, scores, reasoning, summary, error, attempt_count, duration_ms, completed_at, created_at`।
- प्रत्येक **session** आइटम (`sessions`): `session_id, agent_id, environment, status, scores, event_count, started_at, last_event_at, first_event_id, last_event_id, latest_evaluation`।

प्रत्येक कमांड का `--fields` बिल्कुल अपने आइटम के फील्ड नाम को स्वीकार करता है। `sessions` और `evals` के बीच सेट अलग है, इसलिए एक के लिए वैध नाम दूसरे द्वारा अस्वीकार किया जा सकता है।

## अगले कदम

- [CLI](/hi/agenteye/cli): इंस्टॉलेशन, प्रमाणीकरण, और हर कमांड के लिए पूर्ण विकल्प संदर्भ।
- [CLI agent skill](/hi/agenteye/cli-skill): इन रेसिपीज़ को एक कौशल के रूप में पैकेज करें जो आपके कोडिंग एजेंट लोड कर सकते हैं।
- [API keys](/hi/agenteye/api-keys): कुंजियां बनाएँ और स्कोप करें जो CLI, SDK और कलेक्टर का प्रमाणीकरण करते हैं।
- [Python SDK](/hi/agenteye/python-sdk): failproofai Observability में इवेंट भेजें ताकि इन रेसिपीज़ के क्वेरी करने के लिए डेटा हो।