---
title: "이벤트 스트림"
description: "에이전트가 무언가를 하는 순간, 바로 확인할 수 있습니다."
---


에이전트가 무언가를 하는 순간, 바로 확인할 수 있습니다. 이벤트 스트림은 프로덕션의 모든 에이전트를 실시간으로 파악할 수 있는 창구입니다. 기다릴 필요도, 로그를 뒤질 필요도, 무슨 일이 있었는지 추측할 필요도 없습니다.

![실시간 이벤트 스트림: 색상으로 구분된 이벤트 행이 실시간으로 흘러내리며, 환경, 에이전트, 세션, 이벤트 유형, 자유 텍스트로 필터링 가능](/agenteye/images/events-stream.png)

*조직 내 모든 에이전트의 모든 이벤트가 최신순으로 표시되며, 발생하는 즉시 업데이트됩니다.*

## 모든 에이전트를 실시간으로 파악하기

에이전트가 실행을 시작하거나, 모델을 호출하거나, 도구를 실행하거나, 훅을 실행하거나, 오류가 발생하면 해당 행이 발생하는 즉시 스트림 상단에 나타납니다. 조직 내 모든 에이전트의 모든 이벤트를 최신순으로 추적하므로, 오래된 정보가 아닌 현재 상황을 항상 파악할 수 있습니다.

즉, 어딘가의 서버에서 로그 파일을 tail할 필요도, 여러 머신에서 grep을 실행할 필요도, 타임스탬프를 수작업으로 맞출 필요도 없습니다. 페이지 하나를 열면 바로 프로덕션을 모니터링하고 있는 셈입니다.

행은 유형별로 색상이 구분되어 있어 각 줄을 파싱하지 않고도 스트림을 한눈에 파악할 수 있습니다. 각 행에서 한눈에 확인할 수 있는 정보는 다음과 같습니다:

- **유형** (색상 구분): `agent_start`, `model_response`, `tool_use`, `hook_completed`, `error` 등.
- **발생한 내용의 한 줄 요약**: 세부 내용을 열지 않아도 대부분의 상황을 파악할 수 있습니다.
- **해당 단계의 토큰 수**.
- **컨텍스트 윈도우 사용률 배지** (해당하는 경우): 프롬프트 증가와 컴팩션 임박 상황을 문제가 발생하기 전에 미리 파악할 수 있습니다.

실시간으로 모니터링하면 잘못된 배포, 무한 루프, 오류 폭증 등을 다음 날 로그 리뷰에서 발견하는 것이 아니라 발생하는 즉시 포착할 수 있습니다.

## 문제가 된 실행 찾기

뭔가 이상해 보일 때는 모든 데이터를 볼 필요가 없습니다. 문제가 된 단 하나의 실행을 찾으면 됩니다. 스트림은 빠르게 필터링할 수 있습니다: 환경, 에이전트, 세션, 이벤트 유형, 또는 자유 텍스트로 필터링이 가능합니다.

세션 ID나 에이전트 ID로 필터링하면 첫 번째 이벤트부터 마지막 이벤트까지 하나의 실행을 추적할 수 있습니다. 이벤트 유형으로 필터링하면 특정 종류의 활동만 격리해서 볼 수 있습니다. 예를 들어 조직 전체의 모든 `error`를 한 화면에서 확인할 수 있습니다. 필터를 중첩하면 "모든 곳의 모든 것"에서 "이 에이전트, 프로덕션, 오류 발생"으로 몇 번의 클릭만에 좁힐 수 있고, 바로 조치를 취할 수 있습니다.

자유 텍스트 검색을 통해 이미 알고 있는 메시지, 도구 이름, ID를 바로 검색하면 고객 신고 내용에서 정확한 실행으로 몇 초 만에 찾아갈 수 있습니다.

## 찾는 방법

이벤트 스트림은 조직의 홈 화면입니다. 로그인하면 가장 먼저 표시되는 화면으로, `/<org-slug>/`에서 확인할 수 있어 도착하는 즉시 트리아지를 시작할 수 있습니다.

그 뒤에서는 에이전트가 SDK를 통해 이벤트를 전송하고, 콜렉터가 이를 여러분이 직접 제어하는 Failproof AI Observability 서버로 전달하며, 스트림은 이벤트가 도착하는 대로 추적합니다. 원시 데이터 대신 요약된 뷰를 보고 싶다면, 각 실행의 이벤트가 세션에서 단일 행으로 접혀 한 클릭으로 확인할 수 있습니다.

이것은 다른 모든 관측 화면이 기반으로 삼는 근본적인 진실의 원천입니다. 따라서 다른 곳에서 숫자가 이상해 보일 때, 실제로 무슨 일이 있었는지 확인하는 곳은 바로 이 스트림입니다.

## 관련 항목

- [세션](/ko/agenteye/sessions): 동일한 이벤트를 실행별 단일 행으로 집계하며, git 스타일의 실행 그래프를 제공합니다.
- [텔레메트리](/ko/agenteye/telemetry): 에이전트가 전송하는 내용과 이벤트가 스트림에 도달하는 방식.
- [오류 추적](/ko/agenteye/error-tracking): 문제가 발생한 모든 것을 한 곳에서 트리아지할 수 있는 화면.
- [알림](/ko/agenteye/alerts): 임계값을 페이징 규칙으로 전환.
- [CLI 및 에이전트](/ko/agenteye/cli-and-agents): 터미널에서 동일한 실시간 추적.