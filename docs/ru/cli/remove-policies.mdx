---
title: Удаление политик
description: "Удалите записи хука из настроек Claude Code"
---

```bash
failproofai policies --uninstall [policy-names...] [options]
```

Удаляет записи хука failproofai из `settings.json` Claude Code.

Альтернативные команды: `failproofai p -u`

## Параметры

| Флаг | Описание |
|------|----------|
| `--scope user` | Удалить из глобальных настроек (по умолчанию) |
| `--scope project` | Удалить из настроек проекта |
| `--scope local` | Удалить из локальных настроек |
| `--scope all` | Удалить из всех областей одновременно |
| `--custom` / `-c` | Очистить `customPoliciesPath` из конфигурации |

## Поведение

- **Без названий политик** — удаляет все записи хука failproofai из файла настроек
- **С конкретными названиями** — отключает эти политики, но сохраняет установленные хуки

## Примеры

```bash
# Удалить все хуки глобально
failproofai policies --uninstall

# Отключить конкретную политику (сохраняет установленные хуки)
failproofai policies --uninstall block-sudo

# Удалить хуки из всех областей
failproofai policies --uninstall --scope all

# Очистить путь к пользовательским политикам
failproofai policies --uninstall --custom
```