---
title: "Aracılar için CLI tarifleri"
description: "Oturum, olay ve değerlendirme verilerini bir betik veya kodlama aracısının otomatikleştirebileceği şeylere dönüştüren kopyala-yapıştır sorgu desenlerini ve jq tariflerini kullanın."
---


Oturum, olay ve değerlendirme verilerini (ve değerlendirmeleri yeniden tetikleyin) doğrudan bir betikten veya kodlama aracısından çekin — stdout üzerinde temiz JSON, doğrudan `jq` içine boru yapılabilen. Bu tarifler Failproof AI Observability'nin verilerini bir terminal kullanıcısının veya bir AI kodlama aracısının (Claude Code, Cursor) sorgulanabileceği ve otomatikleştirebileceği şeylere dönüştürür, panoyu tıklamadan.

Aşağıdaki desenler Failproof AI Observability CLI'si (`agenteye`) için kopyala-yapıştıra hazırdır. Kurulum, kimlik doğrulama ve tam seçenek listesi için [CLI](/tr/agenteye/cli) bölümüne bakın; yerleşik yardım için `agenteye -h` veya `agenteye <command> -h` komutunu çalıştırın.

## Altın kurallar

1. **Küresel seçenekler komutun *önüne* gider.** `agenteye --json sessions` doğrudur; `agenteye sessions --json` değildir. Küresel seçenekler şunlardır: `--json`, `--base-url`, `--org`, `--token`, `--insecure`/`--secure`, `--timeout`, `--quiet`, `--no-color`.
2. **Çıktıyı ayrıştırdığınızda `--json` geçirin.** Veriler stdout üzerinde JSON olarak gider; insan durumu ve hatalar stderr'e gider, bu nedenle stdout `jq`'ye borulamak için temiz kalır.
3. **Çıkış koduna göre dallandırın**, stderr metni değil: `0` tamam · `1` beklenmeyen hata · `2` kötü argümanlar · `3` panoya ulaşılamıyor · `4` oturum açılmamış veya süresi dolmuş · `5` izin eksik · `6` kaynak bulunamadı.
4. **`-h` ile keşfedin.** Her komut filtrelerini, değer biçimlerini ve JSON şeklini belgeler.

## Tek seferlik kurulum

```bash
export AGENTEYE_DASHBOARD_URL=https://agenteye.example.com   # böylece --base-url tekrarlamanız gerekmez
agenteye login --email you@example.com                       # e-postayla gelen kodu yapıştırın; geçerli ~24s
```

## İşe başlamadan önce kimlik doğrulamayı doğrulayın

`whoami` eksik veya süresi dolmuş bir oturumda hiçbir zaman hata vermez; bunun yerine `logged_in:false` rapor eder, bu nedenle bir aracı kimlik doğrulama durumunu güvenle sondayabilir. (Hiçbir taban URL ayarlanmamışsa veya pano erişilemiyorsa yine de sıfır olmayan bir çıkış kodu verebilir.)

```bash
if [ "$(agenteye --json whoami | jq -r .logged_in)" != "true" ]; then
  echo "Not authenticated. Run: agenteye login" >&2; exit 1
fi
```

## Başarısız veya düşük puanlı oturumları bulun

```bash
# son 24 saatte değerlendirmesi hata ile sonuçlanan oturumlar
agenteye --json sessions --since 24h --status error | jq -r '.sessions[].session_id'

# bir aracı için yardımcılık konusunda <= 0.5 puan alan değerlendirmeler
agenteye --json evals --agent-id checkout-bot --score helpfulness:..0.5 \
  | jq '.evaluations[] | {session_id, scores}'
```

Puan filtrelemesi `evals` üzerinde yaşar, `sessions` üzerinde değil. `--score KEY:MIN..MAX` tekrarlanabilir ve VE-birleştirilmiştir; her iki sınır da isteğe bağlıdır (`..0.5` ≤ 0.5 anlamına gelir, `0.9..` ≥ 0.9 anlamına gelir). İstek başına 20'ye kadar puan filtresi iletebilirsiniz; daha fazlası HTTP 400 döndürür. `sessions`, `--env`, `--status`, `--agent-id`, `--session-id` ve zaman aralığı filtrelerini `evals` ile paylaşır, ancak `--score` yoktur.

## Bir oturumu baştan sona okuyun

Tek bir `session show` komutu yoktur. Olay izini oturumun değerlendirmesiyle birleştirin:

```bash
# oturumun en son değerlendirmesi (durum + puanlar)
agenteye --json evals --session-id run-001 | jq '.evaluations[0] | {status, scores}'

# çalışmadaki her olay (tam tarama için --limit'i yükseltin)
agenteye --json events --session-id run-001 --all --limit 1000 | jq '.events[] | {ts, event_type}'

# bir oturumdaki sadece araç çağrıları (ham payload almak için --full gereklidir)
agenteye --json events --full --session-id run-001 --event-type tool_use,tool_result --all \
  | jq '.events[].payload'
```

> **Not:** Varsayılan olarak, `events` hızlı, yükseklik bilgisiz bir akış okur. Her olay sunucu tarafından hesaplanan bir satırlık `summary` artı `is_error` ve token sayıları gibi bayraklar taşır, ancak `payload` `{}` olarak geri gelir. Ham payload'ı çekmek için `--full` (veya `--fields payload`) ekleyin. Tam akış ölçekte daha yavaştır, bu nedenle sınırlı tutun: `--full` ile tek bir `--session-id` eşleştirin.

## Hepsini getirin (sayfalandırma)

Sonuçlar en yeniden başlayıp imleç ile sayfalandırılır.

```bash
# tek atış: 200 satır sayfalarda 500'e kadar satır getirin
agenteye --json events --session-id run-001 --limit 500 --all > events.json

# manuel sayfalandırma: next_cursor'ı geri besleyin
page=$(agenteye --json events --limit 100)
cursor=$(echo "$page" | jq -r '.next_cursor // empty')
[ -n "$cursor" ] && agenteye --json events --limit 100 --cursor "$cursor"
```

## --fields ile çıktıyı inceletin

Bir aracının okuması gereken şeyi azaltmak için anahtarları (hem tabloda hem de `--json`'da) kısıtlayın.

```bash
agenteye --json sessions --since 7d --fields session_id,status,scores | jq -c '.sessions[]'
agenteye --json events --session-id run-001 --fields ts,event_type --all
```

Bilinmeyen alan adları reddedilir (çıkış `2`), geçerli listeyle, alan adlarını keşfetmek için ucuz bir yol.

## Geçerli filtre değerlerini keşfedin

```bash
agenteye --json list envs | jq -r '.values[]'           # --env için değerler
agenteye --json list tools | jq -r '.values[]'          # araç adları; ayrıca aracılar, modeller, event_types, …
agenteye --json list score_filters | jq -r '.values[]'  # --score KEY:MIN..MAX için geçerli KEY
```

## Kuruluşunuzu seçin (çok kiracılı)

Birden fazla kuruluşa aitse, oturum açma sırasında etkin kiracıyı seçin (kaydedilir):

```bash
agenteye login --org acme --email you@corp.com   # oturum açmayla aynı adımda kiracıyı ayarlayın
agenteye --json orgs list | jq -r '.orgs[].org_slug'
agenteye --org globex --json sessions --since 24h   # bir komut için geçersiz kılın
```

`--org` olmayan çok kuruluşlu oturum açma sıfır olmayan bir çıkış kodu ile çıkar ve seçim yapmak için kuruluşları yazdırır.

## SDK/toplayıcı için bir API anahtarı sağlayın

```bash
# gizli bir kez yazdırılır, --json ile .key alanıdır
key=$(agenteye --json keys create ci-bot --add events:read.add | jq -r '.key')
agenteye keys regenerate ci-bot --yes    # döndürün; iptal etmek için agenteye keys disable ci-bot --yes
```

## Kaydedilmiş veya geçici bir sorguyu çalıştırın

```bash
agenteye --json query run --sql "select count(*) from analytics.events" | jq '.rows'
agenteye --json query run errs --arg prod | jq '.rows'   # kaydedilmiş bir sorgu + konumsal $1
```

## Etkileşimsiz olarak bir olayı sınıflandırın

```bash
id=$(agenteye --json incidents list --state firing | jq -r '.incidents[0].id')
agenteye incidents ack "$id"
agenteye incidents assign "$id" --assignee you@corp.com
agenteye incidents resolve "$id" --yes
```

> **Not:** Mutasyonlar `--json` altında onay istemini otomatik olarak atlar veya stdin TTY değilse, bu nedenle aracılar asla beklemez; başka yerlerde açıkça atlamak için `--yes`/`-y` geçirin.

## Bir betik içinde çıkış kodu işleme

```bash
out=$(agenteye --json sessions --since 1h) || code=$?
case "${code:-0}" in
  0) echo "$out" | jq '.sessions | length' ;;
  4) echo "Session expired - run 'agenteye login'." >&2 ;;
  5) echo "Missing permission (ask an admin for evaluations:read)." >&2 ;;
  3) echo "Dashboard unreachable - check the URL." >&2 ;;
  *) echo "Unexpected error (exit ${code})." >&2 ;;
esac
```

## JSON çıktı şekilleri

| Komut | stdout JSON (ile `--json`) |
|---|---|
| `whoami` | `{"logged_in": true, "id", "email", "is_instance_admin", "active_org", "permissions": [...], "memberships": [...]}` veya `{"logged_in": false}` |
| `orgs list` | `{"active_org", "orgs": [{"org_slug","org_name","permission_set","permissions"}]}` |
| `events` | `{"events": [...], "next_cursor": <cursor or null>}` |
| `evals` | `{"evaluations": [...], "next_cursor": <cursor or null>}` |
| `sessions` | `{"sessions": [...], "next_cursor": <cursor or null>}` |
| `errors` | `{"errors": [...], "next_cursor": <cursor or null>}` |
| `list <kind>` | `{"kind", "values": [...]}` |
| `keys list` / `keys create` | `{"keys": [...]}` / `{id, name, permissions, created_at, key}` (`key` bir kez gösterilir) |
| `query run` | `{columns: [{name,type}], rows: [[...]], truncated, elapsed_ms}` |
| `users list` / `settings list` | `{"users": [...]}` / `{"settings": [...]}` |
| `alerts list` / `incidents list` | `{"alerts": [...]}` / `{"incidents": [...]}` |
| oluştur/güncelle/sil (herhangi biri) | kaynak nesnesi veya silmeler için `{"deleted": true, "id"}` |
| başarısızlık (herhangi biri, `--json` ile) | `{"error": "...", "exit_code": <n>, "status"?: <http>, "hint"?: "..."}` stdout'da |

- Her **olay** öğesi (`events`): `id, session_id, agent_id, event_type, ts, payload, environment, summary, is_error, error_type, output_tokens, context_window, context_fill`. `payload` tam akışı `--full` (veya `--fields payload`) ile istediğiniz sürece `{}` olur.
- Her **değerlendirme** öğesi (`evals`): `id, session_id, agent_id, environment, status, scores, reasoning, summary, error, attempt_count, duration_ms, completed_at, created_at`.
- Her **oturum** öğesi (`sessions`): `session_id, agent_id, environment, status, scores, event_count, started_at, last_event_at, first_event_id, last_event_id, latest_evaluation`.

Her komutun `--fields`'ı tam olarak kendi öğesinin alan adlarını kabul eder. Küme `sessions` ve `evals` arasında farklılık gösterir, bu nedenle biri için geçerli bir ad diğeri tarafından reddedilebilir.

## Sonraki adımlar

- [CLI](/tr/agenteye/cli): kurulum, kimlik doğrulama ve her komut için tam seçenek referansı.
- [CLI aracı becerisi](/tr/agenteye/cli-skill): bu tarifleri kodlama aracınızın yükleyebileceği bir beceri olarak paketleyin.
- [API anahtarları](/tr/agenteye/api-keys): CLI, SDK ve toplayıcı ile kimlik doğrulaması yapan anahtarları oluşturun ve kapsamı belirleyin.
- [Python SDK](/tr/agenteye/python-sdk): Failproof AI Observability'ye olayları gönderin, böylece bu tarifler sorgulanacak veriler olur.