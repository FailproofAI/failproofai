---
title: "Python SDK"
description: "Üretim ortamında AI aracılarınızın tam olarak ne yaptığını görün: her aracı çalışması, araç çağrısı, model isteği, hook ve insan müdahalesi."
---


Üretim ortamında AI aracılarınızın tam olarak ne yaptığını görün: her aracı çalışması, araç çağrısı, model isteği, hook ve insan müdahalesi. Failproof AI Observability Python SDK, aracı kodunuzun içinden bu izleri kaydederek sorun giderme, denetim ve değerlendirme yapabilmenizi sağlar. Failproof AI Observability'nin aracılarınızı gözlemlemesini istediğiniz her zaman bunu kullanın.

Arka planda SDK, yapılandırılmış olayları yerel JSONL dosyalarına yazarken, toplayıcı daemon bunları alıp otomatik olarak platforma gönderir. Bu dosyaları kendiniz yönetmeniz gerekmez.

> **İpucu:** Failproof AI Observability'ye yeni mi başlıyorsunuz? Bu sayfa tam SDK etkinlik referansıdır.

<div style={{ position: "relative", width: "100%", paddingBottom: "56.25%", height: 0, overflow: "hidden", borderRadius: "12px", margin: "1.5rem 0" }}>
  <iframe src="https://www.youtube.com/embed/VWxukZc5k7s?autoplay=1&mute=1&loop=1&playlist=VWxukZc5k7s&rel=0&playsinline=1" title="Watch: Agent Tracing by Failproof AI (2 min)" allow="autoplay; encrypted-media; picture-in-picture; fullscreen" allowFullScreen style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", border: 0 }}></iframe>
</div>

---

## Kurulum

SDK, müşterilere kamu paket indeksinden değil özel bir wheel olarak dağıtılır. Onboarding sürecinde onu nasıl edineceğiniz, yükleyeceğiniz ve sabitleyeceğiniz açıklanır — erişim gerekiyorsa Failproof AI temsilcinizle iletişime geçin.

Yüklendikten sonra, sahip olduğunuzu doğrulayın:

```bash
python -c "import agenteye; print(agenteye.__version__)"
```

Tüm entegrasyonu bir kodlama aracısına yaptırmayı tercih mi ediyorsunuz? [Python SDK Agent Skill](/tr/agenteye/python-sdk-skill), kurulum yolunu bilir, enstrümantasyon noktalarını planlar, yazarlar ve olayların geleceğini doğrular.

---

## Hızlı Başlangıç

```python
import agenteye

agenteye.configure(environment="production")

agenteye.event.agent_start(session_id="run-001", agent_id="planner", goal="answer user query")

agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    input={"query": "latest AI research"},
)

agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    output={"results": ["..."]},
)

agenteye.event.agent_end(session_id="run-001", agent_id="planner", outcome="success")
```

### Gerçek bir çağrıyı enstrümente etme

Pratikte mevcut aracı kodunuzu sarar. Model çağrısını `model_request` ile öncesinde ve `model_response` ile sonrasında parantezleyin, böylece iki olay gerçek isteği kapsar ve Failproof AI Observability bunları eşleştirebilir:

```python
import anthropic
import agenteye

agenteye.configure(environment="production")
client = anthropic.Anthropic()

messages = [{"role": "user", "content": "Summarise today's incidents."}]

agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6",
    messages=messages,
)

reply = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=512,
    messages=messages,
)

agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model=reply.model,
    stop_reason=reply.stop_reason,
    input_tokens=reply.usage.input_tokens,
    output_tokens=reply.usage.output_tokens,
    content=[block.model_dump() for block in reply.content],
)
```

Araç çağrılarını aynı şekilde `tool_use` ve `tool_result` ile sarın, ikili arasında aynı `tool_call_id` kullanın.

Bu olaylar pano ile buluştuktan sonra nasıl göründüğü aşağıdadır; türe göre renklendirilmiş ve ortam, aracı ve oturuma göre filtrelenebilir:

![Canlı Olaylar akışı, olay türüne göre renklendirilmiş ve ortam, aracı ve oturuma göre filtrelenebilir](/agenteye/images/events-stream.png)

---

## configure()

```python
agenteye.configure(
    base_dir=None,           # Path | str | None. Default: $AGENTEYE_HOME or ~/.agenteye
    flush_interval=0.5,      # float, seconds between flush cycles
    environment=None,        # str | None. Deployment environment label
)
```

Herhangi bir `event.*` çağrısından önce bir kez çağırın. Atlamak güvenlidir; varsayılanlar kutudan çıkır çıkmaz çalışır. Tüm bağımsız değişkenler yalnızca anahtar sözcük tarafından verilir; yukarıda gösterildiği gibi ad ile iletin.

`base_dir` `None` (varsayılan) olduğunda, SDK `$AGENTEYE_HOME` okur (ayarlanmışsa),
aksi takdirde `~/.agenteye` öğesine geri döner. Bu, toplayıcının kendi çözümlemesini eşleştirir,
böylece tek bir `AGENTEYE_HOME` env değişkeni hem SDK hem de toplayıcı için paylaşılan olay spoolunu yapılandırır.

---

## Ortam

Her olayı dağıtım ortamı (`production`, `staging`, `qa`, `canary` vb.) ile etiketleyin. Bir kez ayarlayın; SDK bunu otomatik olarak her olaya ekler.

**Seçenek 1: `configure()` aracılığıyla:**

```python
agenteye.configure(environment="production")
```

**Seçenek 2: ortam değişkeni aracılığıyla:**

```bash
export AGENTEYE_ENVIRONMENT=production
```

**Öncelik:** `configure(environment=...)` ortam değişkenini geçersiz kılar. Hiçbiri ayarlanmazsa, `"dev"` öğesine varsayılan olur.

Ortam değişkeni, panoda birinci sınıf bir filtre olarak görünür ve sunucu üzerinde hızlı sorgular için depolanır.

> **Uyarı:** Ortam değerleri hazır virgül `,` içermemelidir. Pano filtreleri tel üzerinde virgülle ayrılmış çoklu seçim kullanır (`?environment=prod,staging`), bu nedenle `prod,blue` olarak adlandırılan bir ortam iki değere bölünür. Virgül içeren ortamlara sahip olaylar alım zamanında reddedilir.

---

## Veri ve Gizlilik

SDK yalnızca açıkça ilettiğiniz alanları kaydeder. İstemleri, mesajları, araç girdilerini ve çıktılarını ve model içeriğini ancak bunları bir `event.*` çağrısına ilettiğiniz için yakalar. İşleminizden hiçbir şey okunmaz veya dolaylı olarak yakalanmaz. Ayarlamadığınız herhangi bir alan olaydan tamamen atlanır; diske yazılmaz.

Bu, redaksyon seçiminiz ve sorumluluğunuz haline getirir. Hızlı tut veya araç yükü PII veya saklamak istemediğiniz sırları içeriyorsa, olay yöntemine geçirmeden önce çıkartın veya maskeyin.

---

## Olay Başvurusu

Çoğu olay, bir korelasyon kimliği paylaşan başlangıç/bitiş çiftlerine gelir: `tool_use` ve `tool_result` bir `tool_call_id` paylaşır, `hook_triggered` ve `hook_completed` bir `hook_id` paylaşır ve `human_wait` ve `human_input` bir `input_id` paylaşır. Başlangıç olayını yayınlayın, işi yapın, ardından aynı kimlikle bitiş olayını yayınlayın. Failproof AI Observability çifti eşleştirir ve `duration_ms` sizin için hesaplar, bu nedenle `duration_ms` hiçbir zaman kendiniz iletmezsiniz.

![Bir oturumun git stilinde yürütme grafiği, olay zaman çizelgesi yanında, eşleştirilmiş olaylardan yeniden oluşturulan, araç/model/hook kesme paneli ile](/agenteye/images/session-detail.png)

Tüm olay yöntemleri bu iki alanı gerektirir:

| Alan | Tür | Açıklama |
|---|---|---|
| `session_id` | `str` | Üst düzey aracı çalışmasını tanımlar |
| `agent_id` | `str` | Olay yayan oturumun hangi aracısını tanımlar |

Tüm yöntemler ayrıca özel meta veri için keyfi `**kwargs` kabul eder ([Özel Alanlar](#özel-alanlar) bölümüne bakın).

---

### `event.agent_start()`

Bir aracı işe başladığında yayınlanır.

```python
agenteye.event.agent_start(
    session_id="run-001",
    agent_id="planner",
    goal="answer user query",   # str | None
    parent_id=None,             # str | None - nested aracılar için parent agent_id
)
```

---

### `event.agent_end()`

Bir aracı işi bitirdiğinde yayınlanır.

```python
agenteye.event.agent_end(
    session_id="run-001",
    agent_id="planner",
    outcome="success",          # str | None
    summary="Answered query",   # str | None
)
```

---

### `event.tool_use()`

Bir aracı bir araç çağırdığında yayınlanır. `tool_result` ile eşleştirin; SDK otomatik olarak `duration_ms` hesaplar.

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",     # str, gerekli
    tool_call_id="toolu_01",    # str, gerekli - eşleşen tool_result için korelasyon anahtarı
    input={"query": "..."},     # dict | None
)
```

---

### `event.tool_result()`

Bir araç döndüğünde yayınlanır. `tool_call_id` aracılığıyla `tool_use` ile ilişkilendirilir.

```python
agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",        # önceki tool_use ile eşleşmelidir
    output={"results": ["..."]},    # Any | None
    error=None,                     # str | None - araç bir istisna oluşturduysa ayarlayın
    # duration_ms otomatik olarak hesaplanır - bunu iletmeyin
)
```

---

### `event.model_request()`

LLM'ye bir istem gönderilmeden hemen önce yayınlanır.

```python
agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - herhangi bir sağlayıcı/model dizesi; doğrulanmaz
    messages=[                   # list[dict] | None - konuşma dönüşleri
        {"role": "user", "content": "..."},
    ],
    system="You are helpful.",   # Any | None - str veya içerik blokları listesi
    tools=[                      # list[dict] | None - modele sunulan araç şemaları
        {"name": "search", "input_schema": {"type": "object"}},
    ],
)
```

`messages` girişleri düz dize `content` veya Anthropic stilinde liste-blok `content` kabul eder. Örnekleme parametreleri (`temperature`, `max_tokens` vb.) ekstra kwargs olarak geçirilebilir.

---

### `event.model_response()`

LLM bir yanıt döndüğünde yayınlanır.

```python
agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - herhangi bir sağlayıcı/model dizesi; doğrulanmaz
    stop_reason="end_turn",     # str | None
    input_tokens=1024,          # int | None
    output_tokens=256,          # int | None
    content=[                    # Any | None - dize veya içerik bloklarının listesi
        {"type": "text", "text": "..."},
    ],
    role="assistant",            # str | None
)
```

`content` düz dize (genel sağlayıcılar) veya Anthropic stilinde içerik blokları listesini kabul eder. Araç çağrıları, ayrı `tool_calls` alanı olmadan `{"type": "tool_use", ...}` blokları olarak `content` içinde yaşarlar.

---

### `event.hook_triggered()`

Bir hook tetiklendiğinde yayınlanır. `hook_completed` ile eşleştirin; SDK otomatik olarak `duration_ms` hesaplar.

```python
agenteye.event.hook_triggered(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",   # str, gerekli
    hook_id="hook-abc",         # str, gerekli - korelasyon anahtarı
    trigger_event="tool_use",   # str | None
    input={"tool": "search"},   # Any | None
)
```

---

### `event.hook_completed()`

Bir hook bittiğinde yayınlanır. `hook_id` aracılığıyla `hook_triggered` ile ilişkilendirilir.

```python
agenteye.event.hook_completed(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",
    hook_id="hook-abc",         # önceki hook_triggered ile eşleşmelidir
    outcome="allow",            # str | None
    output=None,                # Any | None
    error=None,                 # str | None
    # duration_ms otomatik olarak hesaplanır - bunu iletmeyin
)
```

---

### `event.error()`

İşlenmemiş bir hata oluştuğunda yayınlanır.

```python
agenteye.event.error(
    session_id="run-001",
    agent_id="planner",
    error_type="TimeoutError",  # str, gerekli
    message="timed out",        # str, gerekli
    traceback="Traceback...",   # str | None
)
```

---

## İnsan Döngüsü Olayları

İnsan döngüsü olayları, bir kişinin aracının yürütülmesine girdiği anları (onay bekleme, giriş sağlama, aracı durdurma veya durdurma) gözetlemenize olanak tanır. İnsanların yanıt vermesinin ne kadar sürdüğünü ölçmenize (SDK eşleştirilmiş olaylarda `duration_ms` otomatik hesaplar), bir aracıyı kimin durdurduğunu veya kesintiye uğrattığını denetlemenize ve pano yüzeyine çıkan onay ve gözetim iş akışları oluşturmanıza olanak tanır.

### `event.human_wait()`

Aracı, bir insanın giriş sağlamasını beklemek için yürütmeyi durakladığında yayınlanır. `human_input` ile eşleştirin; SDK otomatik olarak `duration_ms` hesaplar (insanın yanıt vermesinin ne kadar sürdüğü).

```python
agenteye.event.human_wait(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",                          # str, gerekli - eşleşen human_input için korelasyon anahtarı
    prompt="Do you approve this action?",        # str | None - insana gösterilen soru
    options=["approve", "reject", "defer"],      # list[str] | None - insana sunulan seçimler
    reason="approval_required",                  # str | None - aracının neden beklediği
)
```

### `event.human_input()`

Bir insan giriş sağladığında ve aracı devam ettiğinde yayınlanır. `input_id` aracılığıyla `human_wait` ile ilişkilendirilir. `duration_ms` otomatik olarak hesaplanır ve arayan tarafından geçirilmemelidir.

```python
agenteye.event.human_input(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",    # str, gerekli - önceki human_wait ile eşleşmelidir
    response="approve",    # str | None - insanın cevabı (serbest metin veya seçilen seçenek)
    # duration_ms otomatik olarak hesaplanır - bunu iletmeyin
)
```

### `event.human_pause()`

Bir insan aracıyı aktif olarak durakladığında (örneğin, pano kontrolü aracılığıyla) yayınlanır. Aracı askıya alınmıştır ancak sonlandırılmamıştır.

```python
agenteye.event.human_pause(
    session_id="run-001",
    agent_id="planner",
    reason="user_requested",  # str | None
    user_id="usr_42",         # str | None - aracıyı duraklatanın kim olduğu
)
```

### `event.human_interrupt()`

Bir insan aracıyı yürütme sırasında aktif olarak durdurduğunda yayınlanır. `human_pause` öğesinden farklı olarak, aracının işi askıya alınmak yerine sonlandırılır.

```python
agenteye.event.human_interrupt(
    session_id="run-001",
    agent_id="planner",
    reason="output_incorrect",       # str | None
    user_id="usr_42",                # str | None - aracıyı kesintiye uğratanın kim olduğu
    at_step="tool_use:web_search",   # str | None - aracı durdurulduğunda ne yapıyordu
)
```

---

## Özel Alanlar

Herhangi bir ekstra anahtar sözcük bağımsız değişken, standart alanlardan sonra olaya eklenir:

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="db_query",
    tool_call_id="toolu_02",
    tenant_id="acme",           # özel alan
    region="us-east-1",         # özel alan
)
```

`timestamp`, `type` ve `environment` ayrılmıştır ve özel alanlar olarak iletilirse `ValueError` oluşturur (`Reserved field names cannot be used as custom fields: [...]`). `session_id` ve `agent_id` her olay yönteminde gerekli parametrelerdir ve ikinci kez sağlanamaz; bunu yaparsanız Python `TypeError` oluşturur. Ortamı `configure(environment=...)` ile ayarlayın (veya `AGENTEYE_ENVIRONMENT` değişkeni).

Alanlarını sorgulamak istediğinizde yükü yapılandırılmış JSON olarak tutun. JSON natively desteklemediği değerler (datetimeler, UUID'ler, ondalıklar, kümeler, baytlar veya model nesneleri gibi) kayıt güvenli şekilde devam etmesi için dizelere dönüştürülür.

---

## Olaylar Nasıl Yazılır

Olaylar işlem içinde arabelleğe alınır ve her `flush_interval` saniyede diske temizlenir (varsayılan 500 ms). Her flush, bir JSONL dosyasını yazar:

```text
~/.agenteye/events/event-2026-04-01T12-00-00-000Z.jsonl
```

Toplayıcı bu dizini izler ve dosyaları otomatik olarak yükler. Bu dosyaları doğrudan yönetmeniz gerekmez.

Her dosya atomik olarak yazılır: SDK geçici bir dosyaya yazar ve ardından yerine taşır, böylece toplayıcı hiçbir zaman yarı yazılmış bir dosya görmez. Son flush ayrıca işleminiz çıktığında çalışır, bu nedenle son aralıkta arabelleğe alınan olaylar kaybolmaz. Toplayıcı çevrimdışıysa, olaylar diska dosya olarak birikir ve tekrar çevrimiçi olduğunda gönderilir.

---

## Sonraki adımlar

- [Olay akışı](/tr/agenteye/event-stream): bu olayları canlı olarak izleyin, olay türüne göre renklendirilmiş ve ortam, aracı ve oturuma göre filtrelenebilir.
- [Oturumlar](/tr/agenteye/sessions): eşleştirilmiş olayların her aracı çalışmasını yürütme grafiği ve zaman çizelgesi olarak nasıl yeniden oluşturduğunu görün.