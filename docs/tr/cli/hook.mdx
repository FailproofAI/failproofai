---
title: Hook işleyicisi (iç)
description: "Her araç olayında Claude Code tarafından çağrılan alt işlem"
---

```bash
failproofai --hook <EventType>
```

Bu, `failproofai policies --install` tarafından Claude Code'un `settings.json` dosyasına kaydedilen komuttur. Normalde bunu doğrudan çağırmazsınız.

stdin'den JSON yükünü okur, etkin olan tüm politikaları değerlendirir ve kararı gösteren bir çıkış kodu ile sonlanır:

| Çıkış kodu | Karar | Etki |
|-----------|-------|------|
| `0` | `allow` | İşlemi izin ver |
| `1` | `deny` | İşlemi engelle - Claude reddetme nedenini görür |
| `2` | `instruct` | Claude'un bağlamına rehberlik ekle |

### Desteklenen olay türleri

| Kategori | Olaylar |
|----------|---------|
| **Araç yürütümü** | `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PermissionRequest`, `PermissionDenied` |
| **Oturum yaşam döngüsü** | `SessionStart`, `SessionEnd`, `Stop`, `StopFailure` |
| **Kullanıcı etkileşimi** | `UserPromptSubmit`, `Notification`, `Elicitation`, `ElicitationResult` |
| **Alt aracılar ve görevler** | `SubagentStart`, `SubagentStop`, `TaskCreated`, `TaskCompleted`, `TeammateIdle` |
| **Yapılandırma** | `InstructionsLoaded`, `ConfigChange`, `CwdChanged` |
| **Dosya sistemi** | `FileChanged`, `WorktreeCreate`, `WorktreeRemove` |
| **Bağlam** | `PreCompact`, `PostCompact` |