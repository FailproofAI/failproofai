---
title: "Modeller"
description: "Model gecikmesini, jetonları, bağlam kullanımını ve trafik dağılımını karşılaştırın."
icon: "cpu"
---

Modeller kullanarak, güvenilirlik veya maliyetin bir model, ajan, ortam veya zaman aralığı ile değişip değişmediğini görebilirsiniz.

<Tabs>
  <Tab title="Dashboard">
    1. **Observe → Models** adresine gidin.
    2. Zaman aralığını ayarlayın, ardından ortam, model, ajan veya oturum kimliğine göre filtreleyin.
    3. Gecikme süresi, token tüketimi, bağlam penceresi kullanımı ve model dağılımını gözden geçirin.
    4. Eşleşen olayları açmak için bir grafik noktasını veya dağılım segmentini seçin.

    ![Gecikme süresi, yüzdelik dilim, token, maliyet, bağlam penceresi ve model dağılımı görünümlerini gösteren Modeller sayfası.](/images/dashboard/models.png)
  </Tab>
  <Tab title="CLI">
    ```bash
    fp list models
    fp events --event-type model_request,model_response --env production --since 24h
    fp --json events --fields ts,agent_id,session_id,event_type,output_tokens,context_fill
    ```

    Özel bir CLI komutu olarak sunulmayan model düzeyindeki toplamalar için kaydedilmiş bir sorgu kullanın.
  </Tab>
</Tabs>