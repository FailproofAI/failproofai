---
title: "Python SDK"
description: "Xem chính xác những gì các agent AI của bạn đã làm trong production: mỗi lần chạy agent, gọi tool, yêu cầu model, hook, và can thiệp của con người."
---


Xem chính xác những gì các agent AI của bạn đã làm trong production: mỗi lần chạy agent, gọi tool, yêu cầu model, hook, và can thiệp của con người. Python SDK Observability của Failproof AI ghi lại toàn bộ quá trình này từ bên trong mã agent của bạn để bạn có thể gỡ lỗi, kiểm toán và đánh giá những gì đã xảy ra. Sử dụng nó bất cứ khi nào bạn muốn Failproof AI Observability quan sát các agent của mình.

Cơ chế hoạt động bên dưới là SDK ghi các sự kiện có cấu trúc vào các tệp JSONL cục bộ, và daemon collector sẽ chọn chúng và tự động gửi đến nền tảng. Bạn không cần quản lý các tệp này.

> **Tip:** Mới làm quen với Failproof AI Observability? Trang này là tài liệu tham khảo đầy đủ về các sự kiện SDK.

<div style={{ position: "relative", width: "100%", paddingBottom: "56.25%", height: 0, overflow: "hidden", borderRadius: "12px", margin: "1.5rem 0" }}>
  <iframe src="https://www.youtube.com/embed/VWxukZc5k7s?autoplay=1&mute=1&loop=1&playlist=VWxukZc5k7s&rel=0&playsinline=1" title="Watch: Agent Tracing by Failproof AI (2 min)" allow="autoplay; encrypted-media; picture-in-picture; fullscreen" allowFullScreen style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", border: 0 }}></iframe>
</div>

---

## Installation

SDK được phân phối cho khách hàng dưới dạng một wheel riêng tư thay vì từ một chỉ mục gói công cộng. Quá trình onboarding của bạn sẽ bao gồm cách lấy nó, cài đặt nó và ghim nó — hãy liên hệ với Failproof AI của bạn nếu bạn cần quyền truy cập.

Sau khi nó được cài đặt, xác nhận rằng bạn có nó:

```bash
python -c "import agenteye; print(agenteye.__version__)"
```

Thích để cho một agent lập trình làm toàn bộ quá trình tích hợp? [Python SDK Agent Skill](/vi/agenteye/python-sdk-skill) biết đường dẫn cài đặt, lập kế hoạch các điểm máy, viết chúng và xác minh các sự kiện đã đến.

---

## Quick Start

```python
import agenteye

agenteye.configure(environment="production")

agenteye.event.agent_start(session_id="run-001", agent_id="planner", goal="answer user query")

agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    input={"query": "latest AI research"},
)

agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    output={"results": ["..."]},
)

agenteye.event.agent_end(session_id="run-001", agent_id="planner", outcome="success")
```

### Instrumenting a real call

Trong thực tế, bạn bao bọc mã agent hiện có của mình. Đặt một lệnh gọi model giữa `model_request` trước và `model_response` sau, để hai sự kiện bao phủ yêu cầu thực và Failproof AI Observability có thể ghép chúng lại:

```python
import anthropic
import agenteye

agenteye.configure(environment="production")
client = anthropic.Anthropic()

messages = [{"role": "user", "content": "Summarise today's incidents."}]

agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6",
    messages=messages,
)

reply = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=512,
    messages=messages,
)

agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model=reply.model,
    stop_reason=reply.stop_reason,
    input_tokens=reply.usage.input_tokens,
    output_tokens=reply.usage.output_tokens,
    content=[block.model_dump() for block in reply.content],
)
```

Bao bọc các lệnh gọi tool theo cách tương tự với `tool_use` và `tool_result`, sử dụng lại một `tool_call_id` trên toàn bộ cặp.

Dưới đây là những sự kiện này trông như thế nào khi chúng đến dashboard, được tô màu theo loại và có thể lọc theo môi trường, agent và session:

![Dòng sự kiện trực tiếp, được tô màu theo loại sự kiện và có thể lọc theo môi trường, agent và session](/agenteye/images/events-stream.png)

---

## configure()

```python
agenteye.configure(
    base_dir=None,           # Path | str | None. Default: $AGENTEYE_HOME or ~/.agenteye
    flush_interval=0.5,      # float, seconds between flush cycles
    environment=None,        # str | None. Deployment environment label
)
```

Gọi một lần trước bất kỳ lệnh gọi `event.*` nào. An toàn để bỏ qua; các giá trị mặc định hoạt động ngay trong hộp. Tất cả các đối số chỉ theo từ khóa; truyền chúng theo tên như hình trên.

Khi `base_dir` là `None` (mặc định), SDK đọc `$AGENTEYE_HOME` nếu được đặt,
nếu không sẽ quay lại `~/.agenteye`. Điều này khớp với độ phân giải của collector,
vì vậy một biến env `AGENTEYE_HOME` duy nhất cấu hình spool sự kiện được chia sẻ cho cả
SDK và collector.

---

## Environment

Gắn nhãn mỗi sự kiện với một môi trường triển khai (`production`, `staging`, `qa`, `canary`, v.v.). Đặt nó một lần; SDK sẽ tự động đính kèm nó vào mỗi sự kiện.

**Tùy chọn 1: qua `configure()`:**

```python
agenteye.configure(environment="production")
```

**Tùy chọn 2: qua biến môi trường:**

```bash
export AGENTEYE_ENVIRONMENT=production
```

**Ưu tiên:** `configure(environment=...)` thắng biến môi trường. Nếu không có cái nào được đặt, mặc định là `"dev"`.

Giá trị môi trường xuất hiện dưới dạng bộ lọc hạng nhất trong dashboard và được lưu trữ trên máy chủ để truy vấn nhanh.

> **Warning:** Các giá trị môi trường không được chứa dấu phẩy `,` theo nghĩa đen. Các bộ lọc dashboard sử dụng đa chọn được phân tách bằng dấu phẩy trên dây (`?environment=prod,staging`), vì vậy một môi trường được đặt tên là `prod,blue` sẽ bị chia thành hai giá trị. Các sự kiện có môi trường chứa dấu phẩy bị từ chối tại thời điểm nhập.

---

## Data and privacy

SDK chỉ ghi lại các trường bạn rõ ràng truyền. Prompt, tin nhắn, đầu vào và đầu ra của tool, và nội dung model chỉ được ghi vì bạn chuyển chúng đến lệnh gọi `event.*`. Không có gì được đọc từ quá trình của bạn hoặc được ghi lại ngầm. Bất kỳ trường nào bạn để không được đặt đều bị loại khỏi sự kiện hoàn toàn; nó không được ghi vào đĩa.

Điều đó làm cho redaction trở thành lựa chọn và trách nhiệm của bạn. Nếu một prompt hoặc payload tool chứa PII hoặc bí mật mà bạn không muốn lưu trữ, hãy loại bỏ hoặc che dấu nó trước khi bạn chuyển nó đến phương thức sự kiện.

---

## Event Reference

Hầu hết các sự kiện đến trong các cặp start/end chia sẻ một ID tương quan: `tool_use` và `tool_result` chia sẻ một `tool_call_id`, `hook_triggered` và `hook_completed` chia sẻ một `hook_id`, và `human_wait` và `human_input` chia sẻ một `input_id`. Phát ra sự kiện bắt đầu, thực hiện công việc, sau đó phát ra sự kiện kết thúc với cùng một ID. Failproof AI Observability khớp với cặp và tính toán `duration_ms` cho bạn, vì vậy bạn không bao giờ chuyển `duration_ms` chính mình.

![Biểu đồ thực thi kiểu git của một session bên cạnh dòng thời gian sự kiện của nó, được xây dựng lại từ các sự kiện ghép đôi, với bảng điều khiển phân tích tool/model/hook](/agenteye/images/session-detail.png)

Tất cả các phương thức sự kiện yêu cầu hai trường này:

| Field | Type | Description |
|---|---|---|
| `session_id` | `str` | Xác định lần chạy agent ở cấp cao nhất |
| `agent_id` | `str` | Xác định agent nào trong session phát ra sự kiện |

Tất cả các phương thức cũng chấp nhận `**kwargs` tùy ý cho siêu dữ liệu tùy chỉnh (xem [Custom Fields](#custom-fields)).

---

### `event.agent_start()`

Được phát ra khi một agent bắt đầu công việc.

```python
agenteye.event.agent_start(
    session_id="run-001",
    agent_id="planner",
    goal="answer user query",   # str | None
    parent_id=None,             # str | None - parent agent_id for nested agents
)
```

---

### `event.agent_end()`

Được phát ra khi một agent kết thúc công việc.

```python
agenteye.event.agent_end(
    session_id="run-001",
    agent_id="planner",
    outcome="success",          # str | None
    summary="Answered query",   # str | None
)
```

---

### `event.tool_use()`

Được phát ra khi một agent gọi một tool. Ghép với `tool_result`; SDK tự động tính `duration_ms`.

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",     # str, required
    tool_call_id="toolu_01",    # str, required - correlation key for the matching tool_result
    input={"query": "..."},     # dict | None
)
```

---

### `event.tool_result()`

Được phát ra khi một tool trả về. Tương quan với `tool_use` qua `tool_call_id`.

```python
agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",        # must match the prior tool_use
    output={"results": ["..."]},    # Any | None
    error=None,                     # str | None - set if the tool raised
    # duration_ms is computed automatically - do not pass it
)
```

---

### `event.model_request()`

Được phát ra ngay trước khi gửi prompt đến một LLM.

```python
agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - any provider/model string; not validated
    messages=[                   # list[dict] | None - conversation turns
        {"role": "user", "content": "..."},
    ],
    system="You are helpful.",   # Any | None - str or list of content blocks
    tools=[                      # list[dict] | None - tool schemas offered to the model
        {"name": "search", "input_schema": {"type": "object"}},
    ],
)
```

Các mục `messages` chấp nhận `content` chuỗi thuần túy hoặc nội dung danh sách kiểu Anthropic `content`. Các tham số lấy mẫu (`temperature`, `max_tokens`, v.v.) có thể được chuyển dưới dạng kwargs bổ sung.

---

### `event.model_response()`

Được phát ra khi LLM trả về một phản hồi.

```python
agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - any provider/model string; not validated
    stop_reason="end_turn",     # str | None
    input_tokens=1024,          # int | None
    output_tokens=256,          # int | None
    content=[                    # Any | None - str, or list of content blocks
        {"type": "text", "text": "..."},
    ],
    role="assistant",            # str | None
)
```

`content` chấp nhận chuỗi thuần túy (nhà cung cấp chung) hoặc danh sách các khối nội dung kiểu Anthropic. Các lệnh gọi tool sống bên trong `content` dưới dạng các khối `{"type": "tool_use", ...}`, không có trường `tool_calls` riêng biệt.

---

### `event.hook_triggered()`

Được phát ra khi một hook kích hoạt. Ghép với `hook_completed`; SDK tự động tính `duration_ms`.

```python
agenteye.event.hook_triggered(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",   # str, required
    hook_id="hook-abc",         # str, required - correlation key
    trigger_event="tool_use",   # str | None
    input={"tool": "search"},   # Any | None
)
```

---

### `event.hook_completed()`

Được phát ra khi một hook hoàn thành. Tương quan với `hook_triggered` qua `hook_id`.

```python
agenteye.event.hook_completed(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",
    hook_id="hook-abc",         # must match the prior hook_triggered
    outcome="allow",            # str | None
    output=None,                # Any | None
    error=None,                 # str | None
    # duration_ms is computed automatically - do not pass it
)
```

---

### `event.error()`

Được phát ra khi một lỗi không được xử lý xảy ra.

```python
agenteye.event.error(
    session_id="run-001",
    agent_id="planner",
    error_type="TimeoutError",  # str, required
    message="timed out",        # str, required
    traceback="Traceback...",   # str | None
)
```

---

## Human-in-the-Loop Events

Các sự kiện human-in-the-loop cung cấp cho bạn sự giám sát những khoảnh khắc mà một người bước vào thực thi của agent (chờ phê duyệt, cung cấp đầu vào, tạm dừng hoặc dừng agent). Chúng cho phép bạn đo lường thời gian con người mất để phản hồi (SDK tự động tính `duration_ms` trên các sự kiện ghép đôi), kiểm toán ai tạm dừng hoặc ngắt agent, và xây dựng các quy trình phê duyệt và giám sát xuất hiện trong dashboard.

### `event.human_wait()`

Được phát ra khi agent tạm dừng thực thi để chờ con người cung cấp đầu vào. Ghép với `human_input`; SDK tự động tính `duration_ms` (thời gian con người mất để phản hồi).

```python
agenteye.event.human_wait(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",                          # str, required - correlation key for the matching human_input
    prompt="Do you approve this action?",        # str | None - the question shown to the human
    options=["approve", "reject", "defer"],      # list[str] | None - choices presented to the human
    reason="approval_required",                  # str | None - why the agent is waiting
)
```

### `event.human_input()`

Được phát ra khi con người cung cấp đầu vào và agent tiếp tục. Tương quan với `human_wait` qua `input_id`. `duration_ms` được tính tự động và không được truyền bởi người gọi.

```python
agenteye.event.human_input(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",    # str, required - must match the prior human_wait
    response="approve",    # str | None - the human's answer (free text or selected option)
    # duration_ms is computed automatically - do not pass it
)
```

### `event.human_pause()`

Được phát ra khi con người chủ động tạm dừng agent (ví dụ: qua một điều khiển dashboard). Agent bị tạm dừng nhưng không bị chấm dứt.

```python
agenteye.event.human_pause(
    session_id="run-001",
    agent_id="planner",
    reason="user_requested",  # str | None
    user_id="usr_42",         # str | None - who paused the agent
)
```

### `event.human_interrupt()`

Được phát ra khi con người chủ động dừng agent giữa lúc thực thi. Không giống như `human_pause`, công việc của agent bị chấm dứt thay vì bị tạm dừng.

```python
agenteye.event.human_interrupt(
    session_id="run-001",
    agent_id="planner",
    reason="output_incorrect",       # str | None
    user_id="usr_42",                # str | None - who interrupted the agent
    at_step="tool_use:web_search",   # str | None - what the agent was doing when stopped
)
```

---

## Custom Fields

Bất kỳ đối số từ khóa bổ sung nào được nối thêm vào sự kiện sau các trường tiêu chuẩn:

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="db_query",
    tool_call_id="toolu_02",
    tenant_id="acme",           # custom field
    region="us-east-1",         # custom field
)
```

`timestamp`, `type` và `environment` được dự trữ và sẽ nâng cao `ValueError` (`Reserved field names cannot be used as custom fields: [...]`) nếu được chuyển làm các trường tùy chỉnh. `session_id` và `agent_id` là các tham số bắt buộc trên mỗi phương thức sự kiện và không thể được cung cấp lần thứ hai; Python sẽ nâng cao `TypeError` nếu bạn làm. Đặt môi trường bằng `configure(environment=...)` (hoặc biến `AGENTEYE_ENVIRONMENT`) thay thế.

Giữ payload dưới dạng JSON có cấu trúc khi bạn muốn truy vấn các trường của chúng. Các giá trị JSON không hỗ trợ natively—chẳng hạn như datetime, UUID, số thập phân, tập hợp, byte hoặc đối tượng model—được chuyển đổi thành chuỗi để việc ghi tiếp tục an toàn.

---

## How Events Are Written

Các sự kiện được đệm trong quá trình và được xả vào đĩa mỗi `flush_interval` giây (mặc định 500 ms). Mỗi lần xả ghi một tệp JSONL:

```text
~/.agenteye/events/event-2026-04-01T12-00-00-000Z.jsonl
```

Collector theo dõi thư mục này và tải các tệp lên tự động. Bạn không cần quản lý các tệp này trực tiếp.

Mỗi tệp được ghi một cách nguyên tử: SDK ghi vào một tệp tạm thời và sau đó đổi tên vào vị trí, vì vậy collector không bao giờ thấy một tệp chưa được ghi xong. Một lần xả cuối cùng cũng chạy khi quá trình của bạn thoát, vì vậy các sự kiện được đệm trong khoảng thời gian cuối cùng không bị mất. Nếu collector ngoại tuyến, các sự kiện đơn giản chỉ tích tụ dưới dạng các tệp trên đĩa và được gửi khi nó quay lại.

---

## Next steps

- [Event stream](/vi/agenteye/event-stream): xem các sự kiện này đến trực tiếp, được tô màu và có thể lọc theo môi trường, agent và session.
- [Sessions](/vi/agenteye/sessions): xem cách các sự kiện ghép đôi tái cấu trúc mỗi lần chạy agent dưới dạng một biểu đồ thực thi và dòng thời gian.