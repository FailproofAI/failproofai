---
title: "Python SDK"
description: "Xem chính xác những gì các agent AI của bạn đã thực hiện trong production: mỗi lần chạy agent, gọi tool, yêu cầu model, hook, và can thiệp của con người."
---


Xem chính xác những gì các agent AI của bạn đã thực hiện trong production: mỗi lần chạy agent, gọi tool, yêu cầu model, hook, và can thiệp của con người. Python SDK Failproof AI Observability ghi lại dấu vết đó từ bên trong mã agent của bạn để bạn có thể debug, kiểm toán, và đánh giá những gì đã xảy ra. Sử dụng nó bất cứ khi nào bạn muốn Failproof AI Observability theo dõi các agent của mình.

Bên trong, SDK ghi các sự kiện có cấu trúc vào các tệp JSONL cục bộ, và daemon collector sẽ nhặt chúng lên và gửi chúng đến platform tự động. Bạn không cần quản lý các tệp đó.

> **Tip:** Mới sử dụng Failproof AI Observability? Trang này là tài liệu tham khảo sự kiện SDK hoàn chỉnh.

<div style={{ position: "relative", width: "100%", paddingBottom: "56.25%", height: 0, overflow: "hidden", borderRadius: "12px", margin: "1.5rem 0" }}>
  <iframe src="https://www.youtube.com/embed/VWxukZc5k7s?autoplay=1&mute=1&loop=1&playlist=VWxukZc5k7s&rel=0&playsinline=1" title="Watch: Agent Tracing by Failproof AI (2 min)" allow="autoplay; encrypted-media; picture-in-picture; fullscreen" allowFullScreen style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", border: 0 }}></iframe>
</div>

---

## Installation

SDK được phân phối cho khách hàng dưới dạng một wheel riêng tư chứ không phải từ một chỉ mục gói công khai. Quá trình onboarding của bạn sẽ hướng dẫn cách lấy, cài đặt và ghim nó — liên hệ với đại diện Failproof AI của bạn nếu bạn cần quyền truy cập.

Sau khi cài đặt, hãy xác nhận rằng bạn có nó:

```bash
python -c "import agenteye; print(agenteye.__version__)"
```

Thích để một agent lập trình làm toàn bộ tích hợp? [Python SDK Agent Skill](/vi/agenteye/python-sdk-skill) biết đường dẫn cài đặt, lập kế hoạch các điểm tính toán, viết chúng và xác minh các sự kiện đến.

---

## Quick Start

```python
import agenteye

agenteye.configure(environment="production")

agenteye.event.agent_start(session_id="run-001", agent_id="planner", goal="answer user query")

agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    input={"query": "latest AI research"},
)

agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    output={"results": ["..."]},
)

agenteye.event.agent_end(session_id="run-001", agent_id="planner", outcome="success")
```

### Instrumenting a real call

Trong thực tế, bạn bao quanh mã agent hiện có của mình. Đặt một lệnh gọi model với `model_request` trước và `model_response` sau, để hai sự kiện kéo dài yêu cầu thực tế và Failproof AI Observability có thể ghép chúng:

```python
import anthropic
import agenteye

agenteye.configure(environment="production")
client = anthropic.Anthropic()

messages = [{"role": "user", "content": "Summarise today's incidents."}]

agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6",
    messages=messages,
)

reply = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=512,
    messages=messages,
)

agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model=reply.model,
    stop_reason=reply.stop_reason,
    input_tokens=reply.usage.input_tokens,
    output_tokens=reply.usage.output_tokens,
    content=[block.model_dump() for block in reply.content],
)
```

Bao quanh các lệnh gọi tool theo cách tương tự với `tool_use` và `tool_result`, sử dụng lại một `tool_call_id` trên cặp.

Đây là cách các sự kiện đó trông như thế nào khi chúng đến dashboard, được mã hóa màu theo loại và có thể lọc theo environment, agent và session:

![The live Events stream, colour-coded by event type and filterable by environment, agent, and session](/agenteye/images/events-stream.png)

---

## configure()

```python
agenteye.configure(
    base_dir=None,           # Path | str | None. Default: $AGENTEYE_HOME or ~/.agenteye
    flush_interval=0.5,      # float, seconds between flush cycles
    environment=None,        # str | None. Deployment environment label
)
```

Gọi một lần trước bất kỳ lệnh gọi `event.*` nào. An toàn để bỏ qua; các giá trị mặc định hoạt động ngay lập tức. Tất cả các đối số chỉ có từ khóa; hãy chuyển chúng theo tên như được hiển thị ở trên.

Khi `base_dir` là `None` (mặc định), SDK đọc `$AGENTEYE_HOME` nếu được đặt, nếu không sẽ quay lại `~/.agenteye`. Điều này phù hợp với độ phân giải của collector, vì vậy một biến env `AGENTEYE_HOME` duy nhất sẽ cấu hình spool sự kiện được chia sẻ cho cả SDK và collector.

---

## Environment

Gắn nhãn mọi sự kiện với một environment triển khai (`production`, `staging`, `qa`, `canary`, v.v.). Đặt nó một lần; SDK sẽ tự động đính kèm nó vào mọi sự kiện.

**Option 1: via `configure()`:**

```python
agenteye.configure(environment="production")
```

**Option 2: via environment variable:**

```bash
export AGENTEYE_ENVIRONMENT=production
```

**Priority:** `configure(environment=...)` thắng biến môi trường. Nếu không có cái nào được đặt, mặc định là `"dev"`.

Giá trị environment xuất hiện như một bộ lọc hạng nhất trong dashboard và được lưu trữ trên máy chủ để truy vấn nhanh.

> **Warning:** Các giá trị Environment không được chứa dấu phẩy `,` theo nghĩa đen. Các bộ lọc dashboard sử dụng multi-select được phân tách bằng dấu phẩy trên dây (`?environment=prod,staging`), vì vậy một environment được đặt tên là `prod,blue` sẽ được chia thành hai giá trị. Các sự kiện có environment chứa dấu phẩy bị từ chối tại thời điểm nhập.

---

## Data and privacy

SDK chỉ ghi lại các trường bạn chuyển truyền một cách rõ ràng. Prompts, messages, tool inputs và outputs, và model content được capture chỉ vì bạn chuyển chúng tới một lệnh gọi `event.*`. Không có gì được đọc từ quá trình của bạn hoặc capture một cách ẩn tàng. Bất kỳ trường nào bạn để chưa được đặt sẽ bị bỏ qua hoàn toàn khỏi sự kiện; nó không được ghi vào đĩa.

Điều đó làm cho redaction là lựa chọn của bạn và trách nhiệm của bạn. Nếu một prompt hoặc tool payload chứa PII hoặc secrets mà bạn không muốn lưu trữ, hãy loại bỏ hoặc che phủ nó trước khi chuyển nó đến phương thức event.

---

## Event Reference

Hầu hết các sự kiện đến theo cặp start/end chia sẻ một ID tương quan: `tool_use` và `tool_result` chia sẻ một `tool_call_id`, `hook_triggered` và `hook_completed` chia sẻ một `hook_id`, và `human_wait` và `human_input` chia sẻ một `input_id`. Phát sự kiện start, thực hiện công việc, sau đó phát sự kiện end với cùng ID. Failproof AI Observability khớp cặp và tính toán `duration_ms` cho bạn, vì vậy bạn không bao giờ tự truyền `duration_ms`.

![A session's git-style execution graph beside its event timeline, reconstructed from the paired events, with the tool/model/hook breakdown panel](/agenteye/images/session-detail.png)

Tất cả các phương thức event yêu cầu hai trường này:

| Field | Type | Description |
|---|---|---|
| `session_id` | `str` | Xác định lần chạy agent cấp cao nhất |
| `agent_id` | `str` | Xác định agent nào trong session phát sự kiện |

Tất cả các phương thức cũng chấp nhận `**kwargs` tùy ý cho siêu dữ liệu tùy chỉnh (xem [Custom Fields](#custom-fields)).

---

### `event.agent_start()`

Phát khi một agent bắt đầu công việc.

```python
agenteye.event.agent_start(
    session_id="run-001",
    agent_id="planner",
    goal="answer user query",   # str | None
    parent_id=None,             # str | None - parent agent_id for nested agents
)
```

---

### `event.agent_end()`

Phát khi một agent hoàn thành công việc.

```python
agenteye.event.agent_end(
    session_id="run-001",
    agent_id="planner",
    outcome="success",          # str | None
    summary="Answered query",   # str | None
)
```

---

### `event.tool_use()`

Phát khi một agent gọi một tool. Ghép với `tool_result`; SDK tự động tính `duration_ms`.

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",     # str, required
    tool_call_id="toolu_01",    # str, required - correlation key for the matching tool_result
    input={"query": "..."},     # dict | None
)
```

---

### `event.tool_result()`

Phát khi một tool trả về. Tương quan với `tool_use` thông qua `tool_call_id`.

```python
agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",        # must match the prior tool_use
    output={"results": ["..."]},    # Any | None
    error=None,                     # str | None - set if the tool raised
    # duration_ms is computed automatically - do not pass it
)
```

---

### `event.model_request()`

Phát ngay trước khi gửi một prompt tới một LLM.

```python
agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - any provider/model string; not validated
    messages=[                   # list[dict] | None - conversation turns
        {"role": "user", "content": "..."},
    ],
    system="You are helpful.",   # Any | None - str or list of content blocks
    tools=[                      # list[dict] | None - tool schemas offered to the model
        {"name": "search", "input_schema": {"type": "object"}},
    ],
)
```

Các mục `messages` chấp nhận `content` chuỗi thuần túy hoặc `content` danh sách các khối Anthropic-style. Các tham số sampling (`temperature`, `max_tokens`, v.v.) có thể được chuyển dưới dạng kwargs bổ sung.

---

### `event.model_response()`

Phát khi LLM trả về một phản hồi.

```python
agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - any provider/model string; not validated
    stop_reason="end_turn",     # str | None
    input_tokens=1024,          # int | None
    output_tokens=256,          # int | None
    content=[                    # Any | None - str, or list of content blocks
        {"type": "text", "text": "..."},
    ],
    role="assistant",            # str | None
)
```

`content` chấp nhận chuỗi thuần túy (các providers chung) hoặc danh sách các khối nội dung Anthropic-style. Các lệnh gọi tool sống bên trong `content` làm các khối `{"type": "tool_use", ...}`, không có trường `tool_calls` riêng biệt.

---

### `event.hook_triggered()`

Phát khi một hook kích hoạt. Ghép với `hook_completed`; SDK tự động tính `duration_ms`.

```python
agenteye.event.hook_triggered(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",   # str, required
    hook_id="hook-abc",         # str, required - correlation key
    trigger_event="tool_use",   # str | None
    input={"tool": "search"},   # Any | None
)
```

---

### `event.hook_completed()`

Phát khi một hook hoàn thành. Tương quan với `hook_triggered` thông qua `hook_id`.

```python
agenteye.event.hook_completed(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",
    hook_id="hook-abc",         # must match the prior hook_triggered
    outcome="allow",            # str | None
    output=None,                # Any | None
    error=None,                 # str | None
    # duration_ms is computed automatically - do not pass it
)
```

---

### `event.error()`

Phát khi một lỗi chưa được xử lý xảy ra.

```python
agenteye.event.error(
    session_id="run-001",
    agent_id="planner",
    error_type="TimeoutError",  # str, required
    message="timed out",        # str, required
    traceback="Traceback...",   # str | None
)
```

---

## Human-in-the-Loop Events

Các sự kiện human-in-the-loop cung cấp cho bạn sự giám sát trên những khoảnh khắc mà một người bước vào quá trình thực thi của agent (chờ phê duyệt, cung cấp input, tạm dừng, hoặc dừng agent). Chúng cho phép bạn đo lường con người mất bao lâu để phản hồi (SDK tự động tính `duration_ms` trên các sự kiện được ghép nối), kiểm toán ai đã tạm dừng hoặc làm gián đoạn một agent, và xây dựng các quy trình phê duyệt và giám sát bề ngoài được hiển thị trong dashboard.

### `event.human_wait()`

Phát khi agent tạm dừng quá trình thực thi để chờ một con người cung cấp input. Ghép với `human_input`; SDK tự động tính `duration_ms` (con người mất bao lâu để phản hồi).

```python
agenteye.event.human_wait(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",                          # str, required - correlation key for the matching human_input
    prompt="Do you approve this action?",        # str | None - the question shown to the human
    options=["approve", "reject", "defer"],      # list[str] | None - choices presented to the human
    reason="approval_required",                  # str | None - why the agent is waiting
)
```

### `event.human_input()`

Phát khi một con người cung cấp input và agent tiếp tục. Tương quan với `human_wait` thông qua `input_id`. `duration_ms` được tính tự động và không được người gọi chuyển.

```python
agenteye.event.human_input(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",    # str, required - must match the prior human_wait
    response="approve",    # str | None - the human's answer (free text or selected option)
    # duration_ms is computed automatically - do not pass it
)
```

### `event.human_pause()`

Phát khi một con người chủ động tạm dừng agent (ví dụ: thông qua điều khiển dashboard). Agent bị tạm dừng nhưng không bị kết thúc.

```python
agenteye.event.human_pause(
    session_id="run-001",
    agent_id="planner",
    reason="user_requested",  # str | None
    user_id="usr_42",         # str | None - who paused the agent
)
```

### `event.human_interrupt()`

Phát khi một con người chủ động dừng agent giữa chừng. Không giống như `human_pause`, công việc của agent bị kết thúc chứ không phải tạm dừng.

```python
agenteye.event.human_interrupt(
    session_id="run-001",
    agent_id="planner",
    reason="output_incorrect",       # str | None
    user_id="usr_42",                # str | None - who interrupted the agent
    at_step="tool_use:web_search",   # str | None - what the agent was doing when stopped
)
```

---

## Custom Fields

Bất kỳ kwargs bổ sung nào được thêm vào sự kiện sau các trường tiêu chuẩn:

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="db_query",
    tool_call_id="toolu_02",
    tenant_id="acme",           # custom field
    region="us-east-1",         # custom field
)
```

`timestamp`, `type`, và `environment` được dành riêng và tăng `ValueError` (`Reserved field names cannot be used as custom fields: [...]`) nếu được chuyển dưới dạng trường tùy chỉnh. `session_id` và `agent_id` là các tham số bắt buộc trên mọi phương thức event và không thể được cung cấp lần thứ hai; Python sẽ tăng `TypeError` nếu bạn làm vậy. Thay vào đó, hãy đặt environment với `configure(environment=...)` (hoặc biến `AGENTEYE_ENVIRONMENT`).

Giữ payloads như JSON có cấu trúc khi bạn muốn truy vấn các trường của chúng. Các giá trị mà JSON không hỗ trợ sẵn — như datetimes, UUIDs, decimals, sets, bytes, hoặc model objects — được chuyển đổi thành chuỗi để recording tiếp tục an toàn.

---

## How Events Are Written

Các sự kiện được đệm trong quy trình và xả vào đĩa mỗi `flush_interval` giây (mặc định 500 ms). Mỗi lần xả ghi một tệp JSONL:

```text
~/.agenteye/events/event-2026-04-01T12-00-00-000Z.jsonl
```

Collector theo dõi thư mục này và tải lên các tệp tự động. Bạn không cần quản lý các tệp này trực tiếp.

Mỗi tệp được ghi nguyên tử: SDK ghi vào một tệp tạm thời rồi đổi tên nó vào vị trí, vì vậy collector không bao giờ nhìn thấy tệp được ghi một nửa. Một lần xả cuối cùng cũng chạy khi quá trình của bạn thoát, vì vậy các sự kiện được đệm trong khoảng thời gian cuối cùng không bị mất. Nếu collector ngoại tuyến, các sự kiện chỉ tích lũy dưới dạng tệp trên đĩa và ship sau khi nó quay lại.

---

## Next steps

- [Event stream](/vi/agenteye/event-stream): xem các sự kiện này đến trực tiếp, được mã hóa màu và có thể lọc theo environment, agent, và session.
- [Sessions](/vi/agenteye/sessions): xem các sự kiện được ghép nối tái cấu trúc từng lần chạy agent dưới dạng biểu đồ thực thi và timeline.