---
title: Gỡ cài đặt chính sách
description: "Loại bỏ các mục hook khỏi cài đặt của Claude Code"
---

```bash
failproofai policies --uninstall [policy-names...] [options]
```

Loại bỏ các mục hook của failproofai khỏi `settings.json` của Claude Code.

Bí danh: `failproofai p -u`

## Tùy chọn

| Cờ | Mô tả |
|------|-------------|
| `--scope user` | Loại bỏ khỏi cài đặt toàn cục (mặc định) |
| `--scope project` | Loại bỏ khỏi cài đặt dự án |
| `--scope local` | Loại bỏ khỏi cài đặt cục bộ |
| `--scope all` | Loại bỏ khỏi tất cả các phạm vi cùng một lúc |
| `--custom` / `-c` | Xóa `customPoliciesPath` khỏi cấu hình |

## Hành vi

- **Không có tên chính sách** - loại bỏ tất cả các mục hook của failproofai khỏi tệp cài đặt
- **Tên cụ thể** - vô hiệu hóa các chính sách đó nhưng vẫn giữ các hook được cài đặt

## Ví dụ

```bash
# Loại bỏ tất cả các hook trên toàn cục
failproofai policies --uninstall

# Vô hiệu hóa một chính sách cụ thể (giữ các hook được cài đặt)
failproofai policies --uninstall block-sudo

# Loại bỏ các hook khỏi mỗi phạm vi
failproofai policies --uninstall --scope all

# Xóa đường dẫn chính sách tùy chỉnh
failproofai policies --uninstall --custom
```