---
title: "Cho các agent"
description: "Thêm kiến thức Failproof AI vào agent mã hóa của bạn chỉ với một lệnh. Hoạt động với Claude Code, Cursor, Windsurf và nhiều ứng dụng khác."
---

Thêm toàn bộ tài liệu tham khảo Failproof AI vào agent mã hóa của bạn chỉ với một lệnh. Hoạt động với Claude Code, Cursor, Windsurf và bất kỳ agent nào khác hỗ trợ skills.

```bash
npx skills add https://docs.befailproof.ai
```

`npx skills` sẽ tự động phát hiện các agent bạn đã cài đặt và thêm skill ở định dạng phù hợp cho từng agent.

## Skill bao gồm những gì

| Lĩnh vực | Nội dung bao gồm |
|---------|-----------------|
| Policies | Tên policy được xây dựng sẵn, loại sự kiện, tham số, bật/tắt |
| Custom policies | `customPolicies.add()`, match filters, API `allow`/`deny`/`instruct` |
| Context object | `ctx.eventType`, `ctx.toolName`, `ctx.toolInput`, `ctx.session` |
| Configuration | Cấu trúc `policies-config.json`, scope merging, `policyParams` |
| CLI | `failproofai policies --install`, `--uninstall`, `--custom`, scopes |
| Dashboard | Session viewer, policy activity, environment variables |
| Architecture | Hook handler flow, exit codes, stdin/stdout contract |

## Skill đã hoàn chỉnh chưa?

Mintlify tạo `llms.txt` từ tất cả các trang trong thanh điều hướng. Tài liệu Failproof AI bao gồm toàn bộ API - mỗi policy, tùy chọn và ví dụ đều có trong đó. Nếu bạn phát hiện thiếu gì, nguồn gốc là `https://docs.befailproof.ai/llms-full.txt`.

Để có ngữ cảnh cụ thể hơn, hãy liên kết trực tiếp đến một trang cụ thể:

```bash
# Chỉ API custom policies
npx skills add https://docs.befailproof.ai/custom-policies

# Chỉ built-in policies
npx skills add https://docs.befailproof.ai/built-in-policies
```