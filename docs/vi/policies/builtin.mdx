---
title: "Các chính sách tích hợp sẵn"
description: "Kích hoạt các hành lang bảo vệ được duy trì cho các chế độ lỗi phổ biến của agent."
icon: "library"
---

Các chính sách tích hợp sẵn bao gồm xử lý bí mật, tệp môi trường, lệnh shell phá huỷ, nhánh được bảo vệ, các công cụ đám mây và cơ sở hạ tầng, công bố gói, các lệnh gọi lặp lại, và kiểm tra quy trình làm việc kết thúc tác vụ.

## Kích hoạt và xác minh một chính sách tích hợp sẵn

<Tabs>
  <Tab title="Dashboard">
    1. Cài đặt chính sách trên một máy được kết nối bằng CLI cục bộ.
    2. Chạy một hành động kiểm tra an toàn trong agent được cấu trúc.
    3. Đi tới **Observe → policy** và lọc theo tên chính sách, môi trường máy hoặc quyết định.
    4. Mở phiên được liên kết để xác nhận đầu vào công cụ phù hợp và lý do được trả về.

  </Tab>
  <Tab title="CLI">
    ```bash
    failproofai policies
    failproofai policy add block-rm-rf --cli claude --scope project
    failproofai config --status
    ```

    Xoá nó bằng `failproofai policy remove block-rm-rf --cli claude --scope project`.
  </Tab>
</Tabs>

Liệt kê các chính sách có sẵn trong phiên bản đã cài đặt của bạn:

```bash
failproofai policies
```

Kích hoạt một chính sách cho một dự án:

```bash
failproofai policy add block-rm-rf --scope project
```

Kích hoạt nhiều chính sách cho các harness được chọn:

```bash
failproofai policies --install block-sudo block-force-push \
  --cli claude codex --scope project
```

Một số chính sách chấp nhận các tham số hoặc được đánh dấu là beta. Hãy xem lại mô tả, phạm vi khớp, và hành vi mặc định trước khi triển khai. Một chính sách bảo vệ một quy trình làm việc có thể chặn các hoạt động hợp lệ trong hoạt động khác.

<Card title="Duyệt danh mục chính sách tích hợp sẵn đầy đủ" icon="list-checks" href="/vi/policies/builtin-catalog">
  Xem lại tất cả 40 chính sách hiện tại, các kích hoạt của chúng, đường cơ sở được đề xuất, và các tham số.
</Card>

<Tip>
  Ưu tiên phạm vi dự án cho các kỳ vọng cụ thể của kho lưu trữ và phạm vi người dùng cho các yêu cầu an toàn trên toàn máy.
</Tip>