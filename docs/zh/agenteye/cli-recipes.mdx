---
title: "面向 Agent 的 CLI 使用指南"
description: "可直接复制粘贴的查询模式与 jq 示例，将会话、事件及评估数据转化为脚本或编码 Agent 可自动化处理的格式。"
---


直接从脚本或编码 Agent 中拉取会话、事件及评估数据（并触发重新评估），所有输出以简洁的 JSON 格式写入 stdout，可直接通过管道传入 `jq`。这些示例将 Failproof AI Observability 的数据转化为终端用户或 AI 编码 Agent（Claude Code、Cursor）可查询和自动化的形式，无需在控制台中点击操作。

以下模式均可直接复制粘贴用于 Failproof AI Observability CLI（`agenteye`）。安装、身份验证及完整选项列表，请参阅 [CLI](/zh/agenteye/cli)；运行 `agenteye -h` 或 `agenteye <command> -h` 可查看内置帮助。

## 基本原则

1. **全局选项须置于命令之前。** `agenteye --json sessions` 正确；`agenteye sessions --json` 不正确。全局选项包括：`--json`、`--base-url`、`--org`、`--token`、`--insecure`/`--secure`、`--timeout`、`--quiet`、`--no-color`。
2. **解析输出时请传入 `--json`。** 数据以 JSON 格式写入 **stdout**；人类可读的状态信息和错误信息写入 **stderr**，因此 stdout 保持干净，可直接管道传入 `jq`。
3. **根据退出码分支处理**，而非依赖 stderr 文本：`0` 成功 · `1` 意外错误 · `2` 参数错误 · `3` 无法连接控制台 · `4` 未登录或已过期 · `5` 权限不足 · `6` 资源不存在。
4. **使用 `-h` 进行探索。** 每个命令均记录了其过滤器、值格式及 JSON 结构。

## 一次性配置

```bash
export AGENTEYE_DASHBOARD_URL=https://agenteye.example.com   # 避免重复填写 --base-url
agenteye login --email you@example.com                       # 粘贴邮件中的验证码；有效期约 24 小时
```

## 执行操作前确认身份验证

`whoami` 在会话缺失或过期时不会报错，而是返回 `logged_in:false`，因此 Agent 可以安全地探测身份验证状态。（若未设置 base URL 或控制台不可达，仍可能以非零状态退出。）

```bash
if [ "$(agenteye --json whoami | jq -r .logged_in)" != "true" ]; then
  echo "Not authenticated. Run: agenteye login" >&2; exit 1
fi
```

## 查找失败或低分会话

```bash
# 过去 24 小时内评估出错的会话
agenteye --json sessions --since 24h --status error | jq -r '.sessions[].session_id'

# 某个 Agent 的帮助性得分 <= 0.5 的评估
agenteye --json evals --agent-id checkout-bot --score helpfulness:..0.5 \
  | jq '.evaluations[] | {session_id, scores}'
```

得分过滤在 **`evals`** 上进行，而非 `sessions`。`--score KEY:MIN..MAX` 可重复使用，多个条件取 AND；任一边界可省略（`..0.5` 表示 ≤ 0.5，`0.9..` 表示 ≥ 0.9）。每次请求最多可传入 20 个得分过滤条件，超出则返回 HTTP 400。`sessions` 与 `evals` 共享 `--env`、`--status`、`--agent-id`、`--session-id` 及时间范围过滤器，但不支持 `--score`。

## 端到端读取单个会话

目前没有单独的 `session show` 命令，可将事件轨迹与会话评估结合使用：

```bash
# 该会话的最新评估（状态 + 得分）
agenteye --json evals --session-id run-001 | jq '.evaluations[0] | {status, scores}'

# 该次运行的所有事件（增大 --limit 以完整扫描）
agenteye --json events --session-id run-001 --all --limit 1000 | jq '.events[] | {ts, event_type}'

# 仅查看会话中的工具调用（需加 --full 以获取原始 payload）
agenteye --json events --full --session-id run-001 --event-type tool_use,tool_result --all \
  | jq '.events[].payload'
```

> **注意：** 默认情况下，`events` 读取的是快速、不含 payload 的数据流。每个事件包含服务器计算的单行 `summary` 以及 `is_error`、token 计数等标志，但 `payload` 返回为 `{}`。如需获取原始 payload，请添加 `--full`（或 `--fields payload`）。完整数据流在大规模场景下较慢，因此请控制范围：将 `--full` 与单个 `--session-id` 配合使用。

## 获取全量数据（分页）

结果按最新优先排序，采用游标分页。

```bash
# 一次性获取：以每页 200 条的方式最多获取 500 条记录
agenteye --json events --session-id run-001 --limit 500 --all > events.json

# 手动分页：将 next_cursor 传回
page=$(agenteye --json events --limit 100)
cursor=$(echo "$page" | jq -r '.next_cursor // empty')
[ -n "$cursor" ] && agenteye --json events --limit 100 --cursor "$cursor"
```

## 使用 --fields 精简输出

限制返回的字段（在表格和 `--json` 输出中均有效），减少 Agent 需要读取的内容。

```bash
agenteye --json sessions --since 7d --fields session_id,status,scores | jq -c '.sessions[]'
agenteye --json events --session-id run-001 --fields ts,event_type --all
```

未知字段名将被拒绝（退出码 `2`），并同时返回有效字段列表，可作为探索字段名的便捷方式。

## 探索有效的过滤器值

```bash
agenteye --json list envs | jq -r '.values[]'           # --env 的有效值
agenteye --json list tools | jq -r '.values[]'          # 工具名称；也支持 agents、models、event_types 等
agenteye --json list score_filters | jq -r '.values[]'  # --score KEY:MIN..MAX 的有效 KEY
```

## 选择组织（多租户）

若您属于多个组织，可在登录时选择当前租户（选择结果会被保存）：

```bash
agenteye login --org acme --email you@corp.com   # 在登录的同时设置租户
agenteye --json orgs list | jq -r '.orgs[].org_slug'
agenteye --org globex --json sessions --since 24h   # 仅对当前命令覆盖租户
```

多组织登录时若未指定 `--org`，将以非零状态退出，并打印可选组织列表。

## 为 SDK/收集器创建 API 密钥

```bash
# 密钥仅打印一次，使用 --json 时位于 .key 字段
key=$(agenteye --json keys create ci-bot --add events:read.add | jq -r '.key')
agenteye keys regenerate ci-bot --yes    # 轮换密钥；agenteye keys disable ci-bot --yes 可吊销密钥
```

## 运行已保存或临时查询

```bash
agenteye --json query run --sql "select count(*) from analytics.events" | jq '.rows'
agenteye --json query run errs --arg prod | jq '.rows'   # 已保存的查询 + 位置参数 $1
```

## 非交互式处理告警事件

```bash
id=$(agenteye --json incidents list --state firing | jq -r '.incidents[0].id')
agenteye incidents ack "$id"
agenteye incidents assign "$id" --assignee you@corp.com
agenteye incidents resolve "$id" --yes
```

> **注意：** 在 `--json` 模式下或 stdin 非 TTY 时，变更操作会自动跳过确认提示，因此 Agent 不会挂起；如需在其他场景下显式跳过，请传入 `--yes`/`-y`。

## 脚本中的退出码处理

```bash
out=$(agenteye --json sessions --since 1h) || code=$?
case "${code:-0}" in
  0) echo "$out" | jq '.sessions | length' ;;
  4) echo "Session expired - run 'agenteye login'." >&2 ;;
  5) echo "Missing permission (ask an admin for evaluations:read)." >&2 ;;
  3) echo "Dashboard unreachable - check the URL." >&2 ;;
  *) echo "Unexpected error (exit ${code})." >&2 ;;
esac
```

## JSON 输出结构

| 命令 | stdout JSON（使用 `--json` 时） |
|---|---|
| `whoami` | `{"logged_in": true, "id", "email", "is_instance_admin", "active_org", "permissions": [...], "memberships": [...]}` 或 `{"logged_in": false}` |
| `orgs list` | `{"active_org", "orgs": [{"org_slug","org_name","permission_set","permissions"}]}` |
| `events` | `{"events": [...], "next_cursor": <cursor or null>}` |
| `evals` | `{"evaluations": [...], "next_cursor": <cursor or null>}` |
| `sessions` | `{"sessions": [...], "next_cursor": <cursor or null>}` |
| `errors` | `{"errors": [...], "next_cursor": <cursor or null>}` |
| `list <kind>` | `{"kind", "values": [...]}` |
| `keys list` / `keys create` | `{"keys": [...]}` / `{id, name, permissions, created_at, key}`（`key` 仅显示一次） |
| `query run` | `{columns: [{name,type}], rows: [[...]], truncated, elapsed_ms}` |
| `users list` / `settings list` | `{"users": [...]}` / `{"settings": [...]}` |
| `alerts list` / `incidents list` | `{"alerts": [...]}` / `{"incidents": [...]}` |
| create/update/delete（任意） | 资源对象，删除操作返回 `{"deleted": true, "id"}` |
| 失败（任意，使用 `--json` 时） | stdout 输出 `{"error": "...", "exit_code": <n>, "status"?: <http>, "hint"?: "..."}` |

- 每个 **event** 条目（`events`）：`id, session_id, agent_id, event_type, ts, payload, environment, summary, is_error, error_type, output_tokens, context_window, context_fill`。注意：除非使用 `--full`（或 `--fields payload`）请求完整数据流，否则 `payload` 为 `{}`。
- 每个 **evaluation** 条目（`evals`）：`id, session_id, agent_id, environment, status, scores, reasoning, summary, error, attempt_count, duration_ms, completed_at, created_at`。
- 每个 **session** 条目（`sessions`）：`session_id, agent_id, environment, status, scores, event_count, started_at, last_event_at, first_event_id, last_event_id, latest_evaluation`。

每个命令的 `--fields` 仅接受其自身条目的字段名。`sessions` 与 `evals` 的字段集不同，对一个命令有效的字段名可能被另一个拒绝。

## 后续步骤

- [CLI](/zh/agenteye/cli)：安装、身份验证及所有命令的完整选项参考。
- [CLI Agent 技能](/zh/agenteye/cli-skill)：将这些示例打包为编码 Agent 可加载的技能。
- [API 密钥](/zh/agenteye/api-keys)：创建并限定 CLI、SDK 及收集器进行身份验证所用密钥的范围。
- [Python SDK](/zh/agenteye/python-sdk)：将事件发送至 Failproof AI Observability，以便这些示例有数据可查询。