---
title: "Python SDK"
description: "精确了解 AI 智能体在生产环境中的行为：每次智能体运行、工具调用、模型请求、hook 及人工干预。"
---


精确了解 AI 智能体在生产环境中的行为：每次智能体运行、工具调用、模型请求、hook 及人工干预。Failproof AI 可观测性 Python SDK 从智能体代码内部记录完整追踪链路，帮助你调试、审计和评估执行过程。当你希望 Failproof AI 可观测性监控你的智能体时，即可使用该 SDK。

在底层，SDK 将结构化事件写入本地 JSONL 文件，采集器守护进程会自动拾取并上传到平台。你无需手动管理这些文件。

> **提示：** 初次使用 Failproof AI 可观测性？本页是完整的 SDK 事件参考文档。

<div style={{ position: "relative", width: "100%", paddingBottom: "56.25%", height: 0, overflow: "hidden", borderRadius: "12px", margin: "1.5rem 0" }}>
  <iframe src="https://www.youtube.com/embed/VWxukZc5k7s?autoplay=1&mute=1&loop=1&playlist=VWxukZc5k7s&rel=0&playsinline=1" title="Watch: Agent Tracing by Failproof AI (2 min)" allow="autoplay; encrypted-media; picture-in-picture; fullscreen" allowFullScreen style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", border: 0 }}></iframe>
</div>

---

## 安装

SDK 以私有 wheel 包的形式分发给客户，而非通过公共包索引。入门流程涵盖获取、安装和版本锁定的方法——如需访问权限，请联系你的 Failproof AI 联系人。

安装完成后，确认安装成功：

```bash
python -c "import agenteye; print(agenteye.__version__)"
```

希望由编码智能体完成整个集成？[Python SDK Agent Skill](/zh/agenteye/python-sdk-skill) 了解安装路径，能够规划埋点位置、编写代码并验证事件是否正确上报。

---

## 快速开始

```python
import agenteye

agenteye.configure(environment="production")

agenteye.event.agent_start(session_id="run-001", agent_id="planner", goal="answer user query")

agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    input={"query": "latest AI research"},
)

agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",
    output={"results": ["..."]},
)

agenteye.event.agent_end(session_id="run-001", agent_id="planner", outcome="success")
```

### 对真实调用进行埋点

实际使用时，你需要在现有智能体代码外层进行包裹。在模型调用前后分别使用 `model_request` 和 `model_response` 进行标记，使这两个事件覆盖真实请求的完整生命周期，Failproof AI 可观测性便可将它们配对关联：

```python
import anthropic
import agenteye

agenteye.configure(environment="production")
client = anthropic.Anthropic()

messages = [{"role": "user", "content": "Summarise today's incidents."}]

agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6",
    messages=messages,
)

reply = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=512,
    messages=messages,
)

agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model=reply.model,
    stop_reason=reply.stop_reason,
    input_tokens=reply.usage.input_tokens,
    output_tokens=reply.usage.output_tokens,
    content=[block.model_dump() for block in reply.content],
)
```

工具调用同理，使用 `tool_use` 和 `tool_result` 进行包裹，并在配对事件中复用同一个 `tool_call_id`。

以下是这些事件在仪表盘中的展示效果——按类型用颜色区分，支持按环境、智能体和会话进行筛选：

![实时事件流，按事件类型用颜色区分，支持按环境、智能体和会话筛选](/agenteye/images/events-stream.png)

---

## configure()

```python
agenteye.configure(
    base_dir=None,           # Path | str | None. 默认：$AGENTEYE_HOME 或 ~/.agenteye
    flush_interval=0.5,      # float，刷新周期间隔（秒）
    environment=None,        # str | None. 部署环境标签
)
```

在任何 `event.*` 调用之前调用一次。可以省略，默认配置开箱即用。所有参数均为仅限关键字参数，请按上方示例以名称形式传入。

当 `base_dir` 为 `None`（默认值）时，SDK 会读取 `$AGENTEYE_HOME`（如已设置），否则回退到 `~/.agenteye`。这与采集器自身的解析逻辑一致，因此只需设置一个 `AGENTEYE_HOME` 环境变量，即可同时为 SDK 和采集器配置共享的事件缓冲目录。

---

## 环境

为每个事件添加部署环境标签（`production`、`staging`、`qa`、`canary` 等）。设置一次后，SDK 会自动将其附加到每个事件上。

**方式一：通过 `configure()` 设置：**

```python
agenteye.configure(environment="production")
```

**方式二：通过环境变量设置：**

```bash
export AGENTEYE_ENVIRONMENT=production
```

**优先级：** `configure(environment=...)` 优先于环境变量。若两者均未设置，默认值为 `"dev"`。

环境值在仪表盘中作为一级筛选项显示，并存储在服务端以支持快速查询。

> **警告：** 环境值不得包含英文逗号 `,`。仪表盘筛选器通过逗号分隔的多选格式传参（`?environment=prod,staging`），因此名为 `prod,blue` 的环境会被拆分为两个值。包含逗号的环境值在数据摄入时会被拒绝。

---

## 数据与隐私

SDK 仅记录你显式传入的字段。提示词、消息、工具输入/输出及模型内容，均仅在你将其传入 `event.*` 调用时才会被捕获。SDK 不会从你的进程中读取任何内容，也不会进行隐式捕获。未设置的字段会被完全省略，不会写入磁盘。

因此，数据脱敏是你的选择，也是你的责任。如果提示词或工具载荷中包含不希望存储的 PII 或密钥，请在传入事件方法之前进行脱敏或掩码处理。

---

## 事件参考

大多数事件以开始/结束配对的形式出现，并共享一个关联 ID：`tool_use` 和 `tool_result` 共享 `tool_call_id`，`hook_triggered` 和 `hook_completed` 共享 `hook_id`，`human_wait` 和 `human_input` 共享 `input_id`。先发送开始事件，执行操作，再以相同 ID 发送结束事件。Failproof AI 可观测性会自动匹配配对并为你计算 `duration_ms`，无需手动传入。

![会话的 git 风格执行图及其事件时间线，由配对事件重建，并附有工具/模型/hook 分布面板](/agenteye/images/session-detail.png)

所有事件方法均需以下两个字段：

| 字段 | 类型 | 描述 |
|---|---|---|
| `session_id` | `str` | 标识顶层智能体运行 |
| `agent_id` | `str` | 标识会话中发出事件的智能体 |

所有方法还接受任意 `**kwargs` 作为自定义元数据（参见[自定义字段](#custom-fields)）。

---

### `event.agent_start()`

智能体开始工作时发出。

```python
agenteye.event.agent_start(
    session_id="run-001",
    agent_id="planner",
    goal="answer user query",   # str | None
    parent_id=None,             # str | None - 嵌套智能体的父级 agent_id
)
```

---

### `event.agent_end()`

智能体完成工作时发出。

```python
agenteye.event.agent_end(
    session_id="run-001",
    agent_id="planner",
    outcome="success",          # str | None
    summary="Answered query",   # str | None
)
```

---

### `event.tool_use()`

智能体调用工具时发出。与 `tool_result` 配对；SDK 自动计算 `duration_ms`。

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",     # str，必填
    tool_call_id="toolu_01",    # str，必填 - 与对应 tool_result 的关联键
    input={"query": "..."},     # dict | None
)
```

---

### `event.tool_result()`

工具返回时发出。通过 `tool_call_id` 与 `tool_use` 关联。

```python
agenteye.event.tool_result(
    session_id="run-001",
    agent_id="planner",
    tool_name="web_search",
    tool_call_id="toolu_01",        # 必须与前面的 tool_use 匹配
    output={"results": ["..."]},    # Any | None
    error=None,                     # str | None - 工具抛出异常时设置
    # duration_ms 自动计算 - 请勿手动传入
)
```

---

### `event.model_request()`

向 LLM 发送提示词之前发出。

```python
agenteye.event.model_request(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - 任意提供商/模型字符串，不做校验
    messages=[                   # list[dict] | None - 对话轮次
        {"role": "user", "content": "..."},
    ],
    system="You are helpful.",   # Any | None - 字符串或内容块列表
    tools=[                      # list[dict] | None - 提供给模型的工具 schema
        {"name": "search", "input_schema": {"type": "object"}},
    ],
)
```

`messages` 条目的 `content` 支持纯字符串或 Anthropic 风格的内容块列表。采样参数（`temperature`、`max_tokens` 等）可作为额外 kwargs 传入。

---

### `event.model_response()`

LLM 返回响应时发出。

```python
agenteye.event.model_response(
    session_id="run-001",
    agent_id="planner",
    model="claude-sonnet-4-6", # str | None - 任意提供商/模型字符串，不做校验
    stop_reason="end_turn",     # str | None
    input_tokens=1024,          # int | None
    output_tokens=256,          # int | None
    content=[                    # Any | None - 字符串或内容块列表
        {"type": "text", "text": "..."},
    ],
    role="assistant",            # str | None
)
```

`content` 支持纯字符串（通用提供商）或 Anthropic 风格的内容块列表。工具调用以 `{"type": "tool_use", ...}` 块的形式存储在 `content` 中，没有单独的 `tool_calls` 字段。

---

### `event.hook_triggered()`

hook 触发时发出。与 `hook_completed` 配对；SDK 自动计算 `duration_ms`。

```python
agenteye.event.hook_triggered(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",   # str，必填
    hook_id="hook-abc",         # str，必填 - 关联键
    trigger_event="tool_use",   # str | None
    input={"tool": "search"},   # Any | None
)
```

---

### `event.hook_completed()`

hook 完成时发出。通过 `hook_id` 与 `hook_triggered` 关联。

```python
agenteye.event.hook_completed(
    session_id="run-001",
    agent_id="planner",
    hook_name="pre_tool_use",
    hook_id="hook-abc",         # 必须与前面的 hook_triggered 匹配
    outcome="allow",            # str | None
    output=None,                # Any | None
    error=None,                 # str | None
    # duration_ms 自动计算 - 请勿手动传入
)
```

---

### `event.error()`

发生未处理错误时发出。

```python
agenteye.event.error(
    session_id="run-001",
    agent_id="planner",
    error_type="TimeoutError",  # str，必填
    message="timed out",        # str，必填
    traceback="Traceback...",   # str | None
)
```

---

## 人工干预事件

人工干预事件让你可以监控人员介入智能体执行过程的时刻（等待审批、提供输入、暂停或停止智能体）。通过这些事件，你可以衡量人员响应所需时长（SDK 自动为配对事件计算 `duration_ms`）、审计谁暂停或中断了智能体，并构建显示在仪表盘中的审批和监督工作流。

### `event.human_wait()`

智能体暂停执行以等待人工输入时发出。与 `human_input` 配对；SDK 自动计算 `duration_ms`（即人员响应所用时长）。

```python
agenteye.event.human_wait(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",                          # str，必填 - 与对应 human_input 的关联键
    prompt="Do you approve this action?",        # str | None - 展示给人员的问题
    options=["approve", "reject", "defer"],      # list[str] | None - 提供给人员的选项
    reason="approval_required",                  # str | None - 智能体等待的原因
)
```

### `event.human_input()`

人员提供输入且智能体恢复执行时发出。通过 `input_id` 与 `human_wait` 关联。`duration_ms` 自动计算，调用方不得传入。

```python
agenteye.event.human_input(
    session_id="run-001",
    agent_id="planner",
    input_id="inp-abc",    # str，必填 - 必须与前面的 human_wait 匹配
    response="approve",    # str | None - 人员的回答（自由文本或所选选项）
    # duration_ms 自动计算 - 请勿手动传入
)
```

### `event.human_pause()`

人员主动暂停智能体时发出（例如通过仪表盘控件）。智能体被挂起，但未终止。

```python
agenteye.event.human_pause(
    session_id="run-001",
    agent_id="planner",
    reason="user_requested",  # str | None
    user_id="usr_42",         # str | None - 执行暂停操作的人员
)
```

### `event.human_interrupt()`

人员在执行过程中主动停止智能体时发出。与 `human_pause` 不同，智能体的工作被终止而非挂起。

```python
agenteye.event.human_interrupt(
    session_id="run-001",
    agent_id="planner",
    reason="output_incorrect",       # str | None
    user_id="usr_42",                # str | None - 执行中断操作的人员
    at_step="tool_use:web_search",   # str | None - 被停止时智能体正在执行的操作
)
```

---

## 自定义字段

任何额外的关键字参数都会在标准字段之后附加到事件中：

```python
agenteye.event.tool_use(
    session_id="run-001",
    agent_id="planner",
    tool_name="db_query",
    tool_call_id="toolu_02",
    tenant_id="acme",           # 自定义字段
    region="us-east-1",         # 自定义字段
)
```

`timestamp`、`type` 和 `environment` 为保留字段，若作为自定义字段传入，会抛出 `ValueError`（`Reserved field names cannot be used as custom fields: [...]`）。`session_id` 和 `agent_id` 是每个事件方法的必填参数，不能重复传入；若重复传入，Python 会抛出 `TypeError`。请使用 `configure(environment=...)` 或 `AGENTEYE_ENVIRONMENT` 变量设置环境值。

当你希望对字段内容进行查询时，建议保持载荷为结构化 JSON 格式。JSON 原生不支持的值类型——如 datetime、UUID、decimal、集合、bytes 或模型对象——会被转换为字符串，以确保记录过程安全进行。

---

## 事件写入机制

事件在进程内缓冲，每隔 `flush_interval` 秒（默认 500 毫秒）刷新一次写入磁盘。每次刷新会写入一个 JSONL 文件：

```text
~/.agenteye/events/event-2026-04-01T12-00-00-000Z.jsonl
```

采集器监控该目录并自动上传文件。你无需直接管理这些文件。

每个文件均以原子方式写入：SDK 先写入临时文件，再重命名到目标位置，因此采集器永远不会读取到写入一半的文件。进程退出时还会执行最终刷新，确保最后一个间隔内缓冲的事件不会丢失。若采集器处于离线状态，事件会以文件形式在磁盘上累积，待采集器恢复后自动上传。

---

## 后续步骤

- [事件流](/zh/agenteye/event-stream)：实时查看事件到达情况，按类型颜色区分，支持按环境、智能体和会话筛选。
- [会话](/zh/agenteye/sessions)：了解配对事件如何将每次智能体运行重建为执行图和时间线。