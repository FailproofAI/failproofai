---
title: "适用于智能体"
description: "一条命令即可将 Failproof AI 知识添加到您的编码智能体。支持 Claude Code、Cursor、Windsurf 等工具。"
---

一条命令即可将完整的 Failproof AI 参考内容添加到您的编码智能体。支持 Claude Code、Cursor、Windsurf 以及任何其他支持技能的智能体。

```bash
npx skills add https://docs.befailproof.ai
```

`npx skills` 会自动检测您已安装的智能体，并以适合各工具的格式添加相应技能。

## 技能涵盖的内容

| 领域 | 包含内容 |
|------|----------------|
| 策略 | 内置策略名称、事件类型、参数、启用/禁用 |
| 自定义策略 | `customPolicies.add()`、匹配过滤器、`allow`/`deny`/`instruct` API |
| 上下文对象 | `ctx.eventType`、`ctx.toolName`、`ctx.toolInput`、`ctx.session` |
| 配置 | `policies-config.json` 结构、作用域合并、`policyParams` |
| CLI | `failproofai policies --install`、`--uninstall`、`--custom`、作用域 |
| 控制台 | 会话查看器、策略活动、环境变量 |
| 架构 | Hook 处理器流程、退出码、stdin/stdout 契约 |

## 技能是否完整？

Mintlify 从导航中所有页面生成 `llms.txt`。Failproof AI 文档涵盖完整 API——每一条策略、选项和示例均已包含。如果您发现有所遗漏，原始内容位于 `https://docs.befailproof.ai/llms-full.txt`。

如需针对性的上下文，可直接链接到特定页面：

```bash
# 仅自定义策略 API
npx skills add https://docs.befailproof.ai/custom-policies

# 仅内置策略
npx skills add https://docs.befailproof.ai/built-in-policies
```