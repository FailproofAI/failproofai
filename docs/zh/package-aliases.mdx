---
title: 包名别名
description: "已注册的拼写错误防护别名及其工作原理"
icon: copy
---

## 官方包

npm 正式包名为 **`failproofai`**：

```bash
npm install -g failproofai
# or
bun add -g failproofai
```

---

## 为何我们持有这些别名

域名抢注（Typosquatting）是一种常见的供应链攻击手段——恶意攻击者会注册与热门包名仅差一个按键的包名。粗心的用户在输入安装命令时一旦拼错，就会运行攻击者控制的代码，并获得完整的系统访问权限。这正是 Failproof AI 所致力于防范的威胁。

为彻底消除这一攻击面，**我们在 npm 上提前抢占了 `failproofai` 所有常见的拼写错误变体和格式变体**。任何第三方均无法注册这些包名。每个别名包都是一个轻量级代理，安装后会委托给真正的 `failproofai` 包执行。

---

## 已注册别名

**格式变体** — "failproof ai" 的各种写法：

| 包名 | 状态 |
|---------|--------|
| `failproof` | ✅ 已发布 |
| `failproof-ai` | ⏳ 等待 npm 审核 |
| `fail-proof-ai` | ⏳ 等待 npm 审核 |
| `failproof_ai` | ⏳ 等待 npm 审核 |
| `fail_proof_ai` | ⏳ 等待 npm 审核 |
| `fail-proofai` | ⏳ 等待 npm 审核 |

**`failprof*` 拼写错误** — "proof" 中少了一个 `o`：

| 包名 | 状态 |
|---------|--------|
| `failprof` | ✅ 已发布 |
| `failprof-ai` | ✅ 已发布 |
| `failprofai` | ⏳ 等待 npm 审核 |
| `fail-prof-ai` | ⏳ 等待 npm 审核 |
| `failprof_ai` | ⏳ 等待 npm 审核 |

**`faliproof*` 拼写错误** — `a` 和 `i` 位置互换：

| 包名 | 状态 |
|---------|--------|
| `faliproof` | ✅ 已发布 |
| `faliproof-ai` | ✅ 已发布 |
| `faliproofai` | ⏳ 等待 npm 审核 |

> **为何显示"等待审核"？** npm 的反垃圾包策略会拦截在去除标点并进行相似度检测后与已有包名规范化结果相同的包名。我们已联系 npm 支持团队，申请以反抢注为目的保留这些名称，待审核通过后即可激活。

您可以通过以下命令验证任意已发布别名的所有权归属：

```bash
npm info failproof
# Look for: "ExosphereHost Inc." in the maintainers field
```

---

## 别名的工作原理

每个别名包：

1. 将 `failproofai` 列为依赖项——这样真正的包会被安装，其可执行文件也随之可用
2. 暴露一个与自身包名匹配的可执行文件（例如 `failprof-ai`），将所有参数代理转发给 `failproofai` 的可执行文件

代理逻辑仅为两行 Node 脚本，不含任何业务逻辑、网络请求，也不会进行超出 `failproofai` 本身范围的数据收集。

---

## 如果发现我们遗漏的包名

请在 [failproofai/failproofai](https://github.com/failproofai/failproofai/issues) 提交 Issue，我们将及时注册。