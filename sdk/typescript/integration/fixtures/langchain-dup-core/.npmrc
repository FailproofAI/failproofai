# Install the vendored provider as a real copy (node_modules/lc-weather-provider,
# its own core nested beneath it) rather than a symlink: the layout npm gives a
# provider fetched from the registry.
install-links=true
