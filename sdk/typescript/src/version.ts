// The version this package publishes under. Mirrors `failproofai_sdk/_version.py`
// in the Python SDK, but the two version INDEPENDENTLY — npm and PyPI have
// different pre-release grammars and different release cadences, and nothing
// downstream compares them. See CHANGELOG.md for this package's own history.
export const VERSION = "0.0.1-beta.1";
